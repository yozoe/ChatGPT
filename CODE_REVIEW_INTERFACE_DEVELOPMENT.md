# Codex 风格审查界面开发文档

> 状态：界面规格，尚未实现
> 适用范围：Codex Desk macOS Flutter 工作台
> 参考：Codex 桌面客户端右侧“审查”界面

## 1. 界面目标

将现有模态“审查”弹窗改为工作台右侧的常驻审查界面。用户打开审查后仍能看到并操作当前会话，关闭审查后返回原有环境信息检查器，不丢失 Composer 草稿、附件或时间线位置。

界面必须支持：

- 同一纵向滚动区连续显示多个文件的 Diff；
- 右侧目录树筛选、选择文件并跳转到对应 Diff；
- 正文滚动时同步选中文件树中的当前文件；
- 显示文件状态、文件级和总计新增/删除行数；
- 显示 Hunk、旧/新行号、未修改行折叠提示和代码内容；
- 在真实 Git 能力可用时进入暂存、还原、提交和推送操作；
- 在宽窗口并排显示，在窄窗口切换为工作台内覆盖页。

该界面不是独立 macOS 窗口，也不是新的全屏路由；它属于当前会话工作台的右侧检查区域。

## 2. 页面结构

### 2.1 宽窗口

```text
┌──────────────┬──────────────────────────┬──────────────────────────────────────┐
│ 项目栏       │ 会话工作区               │ 审查                                 │
│              │                          ├──────────────────────────────────────┤
│              │                          │ 仓库  来源  +328 -17       操作      │
│              │                          ├────────────────────────┬─────────────┤
│              │                          │                        │ 筛选文件…  │
│              │                          │ 连续文件 Diff          ├─────────────┤
│              │                          │                        │ 目录树      │
│              │                          │                        │             │
│              │                          │                        │             │
└──────────────┴──────────────────────────┴────────────────────────┴─────────────┘
```

审查界面由四个固定区域组成：

1. 审查标题栏；
2. 审查工具栏；
3. 连续 Diff 正文；
4. 文件导航栏。

### 2.2 窄窗口

当当前工作台不足以同时保证会话区 520px 和审查区 740px（正文 520px、分隔线、文件导航 220px）时，审查不再与会话并排：

- 审查覆盖会话工作区，不压缩成不可操作的三列；
- 项目栏按现有规则保留或收起；
- 标题栏左侧显示返回按钮；
- `Esc` 或返回按钮关闭审查并回到原会话；
- 文件导航通过工具栏按钮显示为右侧覆盖层；
- 切换宽窄布局不重建会话、Composer 或审查滚动状态。

## 3. 尺寸与布局规则

| 区域 | 建议尺寸 | 规则 |
|---|---:|---|
| 审查总宽度 | 560–960px | 通过现有分隔条拖动；低于 740px 时文件导航必须隐藏或改为覆盖层 |
| Diff 正文宽度 | 最小 520px | 文件导航常驻时仍须满足；不能用压缩正文换取三列同时显示 |
| 标题栏高度 | 36px | 与 Codex 顶部标签栏密度一致 |
| 工具栏高度 | 42px | 单行排列，空间不足时优先隐藏低频文字 |
| 文件导航宽度 | 220–280px | 默认 240px，可通过按钮整体隐藏 |
| 文件筛选框高度 | 30px | 紧凑输入框，不使用大面积卡片 |
| 文件标题高度 | 最小 36px | 路径过长时单行渐隐或省略，tooltip 显示完整路径 |
| 未修改行提示 | 28px | 横跨正文，低对比背景，可点击时显示悬停反馈 |
| Diff 行高 | 20px | 等宽字体 12px，保证高密度代码阅读 |
| 行号栏 | 每列 44px | 右对齐、不可挤压正文 |

布局使用 `Row`、`Column`、`CustomScrollView` 和 Sliver，不用绝对定位承载正文、文件树或主要操作。

## 4. 标题栏

标题栏负责说明当前右侧工作区的身份，不承载 Git 状态筛选。

### 4.1 元素

- 左侧：审查图标和文字“审查”；
- 右侧：关闭按钮；
- 窄窗口：最左侧增加返回按钮；
- 关闭按钮点击面不小于 28×28px，并提供“关闭审查”tooltip。

### 4.2 行为

- 点击关闭恢复环境信息检查器；
- 如果审查期间打开子智能体检查器，子智能体临时覆盖审查；关闭子智能体后返回审查；
- 首版不显示没有实际功能的“新建标签页”加号；未来具备多检查页能力后再增加。

## 5. 工具栏

### 5.1 左侧信息

从左到右：

1. 仓库选择器；
2. 变更来源选择器；
3. 总新增行数；
4. 总删除行数。

首版来源：

- `最新一轮`：当前任务最后一轮可靠的 `fileChanges` / `turnDiff`；
- `Git 工作区`：当前仓库实时的暂存、未暂存和未跟踪改动。

只有一个真实仓库时直接显示仓库名，不显示可点击箭头；不在多仓库能力完成前展示虚假的“所有仓库”。

统计规则：

- 新增使用现有成功色，格式 `+328`；
- 删除使用现有错误色，格式 `-17`；
- 无法可靠统计时显示 `+?` / `-?`；
- binary 或仅元数据变更不能计为 `+0 -0`。

### 5.2 右侧操作

- 刷新：重新读取当前来源；
- 文件导航显隐：显示或隐藏右侧文件树；
- 更多：容纳低频显示选项；
- 提交或推送：复用现有显式 Git 操作。

“提交或推送”只在 Git 仓库和对应操作可用时启用。它不自动暂存文件，不执行 force push。所有图标按钮必须有 tooltip、焦点样式和语义标签。

## 6. 连续 Diff 正文

### 6.1 文件区块

每个文件按以下顺序显示：

```text
文件状态图标  相对路径                         +新增 -删除
未修改 N 行
@@ -旧起始,数量 +新起始,数量 @@
旧行号  新行号  代码
```

文件标题使用粘性标题：文件仍占据当前视口主要区域时，标题停留在正文顶部；进入下一文件后更新。

状态图标必须区分：

- 修改；
- 新增；
- 删除；
- 重命名；
- 复制；
- binary；
- Diff 不可用或读取失败。

### 6.2 Diff 行

每行由三列组成：

1. 旧文件行号；
2. 新文件行号；
3. 代码内容。

显示规则：

- 上下文行同时显示旧、新行号；
- 删除行只显示旧行号，使用低饱和红色背景；
- 新增行只显示新行号，使用低饱和绿色背景；
- Hunk 标题使用强调色文字和独立低对比背景；
- `No newline at end of file` 使用辅助文本样式，不伪装成代码；
- 代码内容可选择复制，行号不混入复制文本；
- 超长代码行允许横向滚动，不能扩大整个窗口或截断其他区域；行号栏固定，所有可见代码行使用审查画布级共享横向 ScrollController，禁止每行各自滚动；
- 语法无法识别时仍保留 Diff 语义色和等宽排版。

### 6.3 未修改区域

- 首个 Hunk 前以及相邻 Hunk 之间显示“未修改 N 行”；首段数量由首个 Hunk 的旧/新起始行计算，两个 Hunk 之间按前一段结束与后一段起始计算；
- 只有取得可靠文件总行数时才显示最后一个 Hunk 后的未修改行数，不能用当前磁盘文件长度冒充历史任务快照；
- 可以可靠读取额外上下文时，点击展开；
- 无法还原历史上下文时保持不可展开，并通过 tooltip 说明；
- 展开前后保持当前视口锚点，不能让用户突然跳离正在阅读的代码。

### 6.4 滚动

- 全部文件共用一个纵向 ScrollController；代码内容共用一个横向 ScrollController，旧/新行号栏固定在横向视口外；
- 不为每个文件创建嵌套纵向滚动区；
- 文件、Hunk 和 Diff 行使用稳定 Key；
- 文件树跳转采用直接定位，不使用长时间补间动画；
- 用户手动滚动正文时，根据最接近视口顶部的文件标题同步当前文件；
- 刷新后优先恢复同一文件和相近 Hunk；目标不存在时回到第一个变更文件。

## 7. 文件导航栏

### 7.1 筛选框

- placeholder：`筛选文件…`；
- 按相对路径大小写不敏感匹配；
- 输入时立即更新树，不修改原始文件顺序；
- `Esc` 先清空筛选；筛选为空时再按 `Esc` 关闭覆盖层或审查页；
- 无结果时显示“没有匹配的文件”。

### 7.2 目录树

- 以仓库相对路径按目录分组；
- 单子目录可以保留层级，不做可能令人误解的路径压缩；
- 目录默认展开到当前选中文件；
- 文件行显示状态图标、文件名和行数统计；
- 当前文件使用低对比选中背景，不只依靠文字颜色；
- 点击文件后正文定位，并确保该文件行仍在导航栏可见区域；
- 目录展开状态只属于当前审查会话，切换项目后清空；
- 长路径通过末端渐隐或省略处理，tooltip 显示完整相对路径。

### 7.3 行尾操作

Git 工作区来源允许在文件行悬停时显示已有的暂存/还原入口：

- 暂存为普通写操作，点击后在发起行显示进行中状态并刷新；
- 还原必须二次确认；未跟踪文件明确提示会删除磁盘文件；
- 最新一轮来源保持只读，不在文件行显示 Git 写操作；
- 操作按钮出现时不能改变文件名可用宽度或造成整行跳动；
- 复用现有全局 `gitOperationRunning`：任一 Git 写操作进行时禁用全部文件行及工具栏写入口，只在发起行显示具体进度，避免其他按钮看似可用但操作被控制器拒绝。

## 8. 页面状态

| 状态 | 正文 | 文件导航 | 工具栏 |
|---|---|---|---|
| 初次加载 | 居中进度提示 | 骨架或禁用 | 刷新禁用 |
| 有变更 | 连续 Diff | 目录树 | 统计和操作可用 |
| 无变更 | “当前范围没有文件变更” | 空列表 | 保留来源切换 |
| 非 Git 项目 | 说明当前项目不是 Git 仓库 | 隐藏 | Git 操作禁用 |
| 单文件失败 | 文件区块内错误和重试 | 文件保留错误状态 | 其他文件仍可用 |
| 全局失败 | 错误说明和重试 | 禁用 | 只保留关闭/重试 |
| Diff 截断 | 已加载内容和显式警告 | 文件显示警告标记 | 不伪装为完整统计 |
| Git 操作中 | 保留现有 Diff | 禁用全部 Git 写入口，发起行显示进度 | 工具栏写操作全局禁用 |

空状态、错误状态和加载状态均在审查区内部呈现，不弹出阻断会话的对话框。

## 9. 交互规则

| 用户动作 | 结果 |
|---|---|
| 点击任务摘要“审核” | 打开审查并选择“最新一轮” |
| 点击环境信息“变更” | 打开审查并选择“Git 工作区” |
| 点击文件树文件 | 正文跳转到对应文件标题 |
| 滚动正文进入另一文件 | 文件树同步选择并滚入可见区 |
| 隐藏文件导航 | 正文扩展，滚动位置不变 |
| 切换来源 | 保留各来源自己的筛选、选择和滚动位置 |
| 切换任务 | 面板保持打开，加载新任务数据，旧异步结果失效 |
| 切换项目 | 清空旧审查状态并加载新项目 |
| 打开子智能体检查器 | 临时替换审查界面 |
| 关闭子智能体检查器 | 返回此前审查状态 |
| 调整窗口宽度 | 并排/覆盖模式切换，草稿与审查位置保持 |
| 按 `Esc` | 依次清空筛选、关闭文件树覆盖层、关闭审查 |

## 10. 视觉规范

- 直接复用 `YeknomPalette` 的工作台、边框、字段、选中、成功、错误、警告和强调色；
- 不给审查正文套大圆角卡片，主要层级由表面、细分隔线和间距建立；
- 分隔线使用 1px 低对比边框；
- 正文等宽字体 12px/20px，辅助信息 11–12px，工具栏文字 12–13px；
- 常用图标 15–17px，关闭和主要操作可以使用 18px；
- 新增/删除背景必须在深浅主题下均保持文字可读，不使用高饱和整行色块；
- 悬停反馈延续项目统一的 450ms tooltip 延迟；
- 选中、悬停、键盘焦点是三种独立状态；
- 禁用态不能只降低透明度到无法辨认，仍需满足辅助信息可读性。

## 11. 键盘与辅助功能

- `Esc`：按当前层级返回；
- `Tab` / `Shift+Tab`：按标题栏、工具栏、筛选框、文件树、正文的逻辑顺序移动；
- 文件树获得焦点时，上下方向键移动文件，左右方向键展开/收起目录，Enter 打开文件；
- 每个文件区块提供“文件路径、状态、新增、删除”的组合语义；
- Diff 行语义包含旧行号、新行号和变更类型；
- 图标按钮全部提供文字 tooltip 和读屏 label；
- 系统减少动态效果时禁用非必要滚动动画；
- 颜色不作为新增、删除、选中或错误的唯一辨识手段。

## 12. Flutter 组件拆分

建议从 `codex_workspace.dart` 提取界面组件：

```text
lib/src/presentation/review/review_workspace.dart
lib/src/presentation/review/review_title_bar.dart
lib/src/presentation/review/review_toolbar.dart
lib/src/presentation/review/review_diff_canvas.dart
lib/src/presentation/review/review_file_section.dart
lib/src/presentation/review/review_diff_line.dart
lib/src/presentation/review/review_file_navigator.dart
lib/src/presentation/review/review_empty_state.dart
lib/src/presentation/review/review_error_state.dart
```

组件职责：

- `ReviewWorkspace`：宽窄布局、区域组合和页面级焦点；
- `ReviewTitleBar`：标题、返回和关闭；
- `ReviewToolbar`：仓库、来源、统计和操作；
- `ReviewDiffCanvas`：唯一纵向滚动容器、画布级共享横向控制器、固定行号栏、文件锚点和可见文件同步；
- `ReviewFileSection`：文件标题、状态、Hunk 和截断反馈；
- `ReviewDiffLine`：行号、Diff 语义和代码选择；
- `ReviewFileNavigator`：筛选、目录树、选择及行尾操作。

跨工具栏、正文和文件树共享的异步状态使用 Riverpod；悬停、临时焦点和单个目录展开等生命周期明确的交互状态可以留在局部 Widget。

## 13. 界面实施顺序

### UI-1：右侧工作区壳层

- 增加审查打开/关闭状态；
- 替换模态弹窗入口；
- 完成宽窗口并排和窄窗口覆盖布局；
- 保证 Composer、时间线及子智能体检查器生命周期。

### UI-2：连续 Diff 正文

- 实现文件标题、Hunk、行号、增删行和未修改区域；
- 使用 Sliver 惰性构建长 Diff；
- 完成空、加载、错误、binary 和截断状态。

### UI-3：文件导航与联动

- 实现筛选框和目录树；
- 实现点击跳转、正文滚动同步选择和显隐；
- 完成长路径、大量文件和键盘导航。

### UI-4：Git 操作与视觉收口

- 将已有暂存、还原、提交和推送操作接入对应位置；
- 完成深浅主题、悬停、焦点、语义和截图对比；
- 移除旧审查弹窗的重复界面。

## 14. Widget 测试清单

- 宽窗口显示会话与审查，窄窗口显示覆盖页；
- 拖动分隔条不会突破会话和审查最小宽度；
- 窗口缩放跨过布局阈值时保持 Composer 草稿和审查滚动位置；
- 从任务摘要和环境信息进入时选择正确来源；
- 打开/关闭审查不重建会话时间线；
- 文件树点击与正文滚动双向联动；
- 隐藏/显示文件树不改变正文锚点；
- 横向滚动任一代码行时所有可见代码行保持同一偏移，旧/新行号栏固定；
- 首个 Hunk 前、Hunk 之间以及具备可靠总行数时的末尾折叠数量正确；
- 筛选无结果、长路径、超长代码行和大量文件不溢出；
- 加载、空、单文件失败、全局失败、binary 和截断状态正确；
- 项目、任务和来源切换后旧异步结果不能回写；
- 子智能体检查器关闭后恢复此前审查状态；
- 任一暂存或还原进行中时全部 Git 写入口禁用，发起行显示进度，确认框和错误反馈正确；
- `Esc`、Tab 顺序、方向键、tooltip 和语义标签正确。

涉及窗口边界、悬停或异步刷新时必须覆盖对应生命周期测试。

## 15. 视觉验收标准

- 审查界面与会话并排时形成清晰三列，不像覆盖在应用上的大弹窗；
- 标题栏、工具栏、正文和文件树的信息密度接近参考截图；
- 主要阅读路径始终是文件标题 → Hunk → Diff 行，操作按钮不抢占代码注意力；
- 新增、删除、上下文、Hunk、选中和错误状态无需反复辨认；
- 文件树和正文的当前文件始终一致；
- 10,000 行级 Diff 滚动时不出现明显整页重建或长时间卡顿；
- 深浅主题下无文字重叠、裁切、意外横向页面滚动或低对比内容；
- 窄窗口有明确返回路径，所有核心操作仍可到达；
- 实际运行截图与参考界面逐项对照通过后，才视为界面交付。
