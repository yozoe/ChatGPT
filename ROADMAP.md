# Codex Desk 路线图

本文档是项目唯一的后续任务清单。状态约定：`已完成`、`进行中`、`待办`、`受阻`。每完成一个任务，应同步更新本文档和 README。

## 已完成基线

- `已完成` 将环境信息检查器与审查/子智能体工作区解耦；收起工作区时环境信息仍固定显示在窗口右侧。
- `已完成` 修复剪贴板截图在 Hot Restart 后从历史消息消失的问题：图片在任务或方向真正发送前转存到应用专用持久目录，时间线、运行时输入与按线程保存的加密历史均引用该副本；切换到其他任务、缓存淘汰或应用重启后重新打开原任务仍会恢复缩略图，恢复后的线程可继续追加消息。多图转存失败会回滚已创建文件，方向请求被拒绝会清除未引用副本，失败任务重试复用原副本；草稿中未发送的临时截图仍按附件引用关系清理。历史快照为 AES-GCM，加密边界明确不涵盖为 Flutter 本地预览保留的图片字节。
- `已完成` 修复并发子智能体在界面中被遗漏的问题：App Server 的旧版 `collabToolCall` 与当前 `subAgentActivity` 启动事件均单独保留；主会话时间线即时以头像、子任务名称和“已开始工作”的紧凑提示呈现全部运行项，完成后原位更新为历史活动而不重复；右侧“子智能体”摘要即时显示全部运行项，目录按“已开启”和“完成”展示，不再只保留最后一个活动。
- `已完成` 增加外层协作运行时桥接：工作区 `.codex/codex-desk-collaboration.json` 的活动清单必须带与当前会话一致的 `parentThreadId`（或兼容别名），客户端会持久化该归属；无法证明归属的旧活动会被清理，避免跨会话串线，同时保留已归属但没有子线程 ID 的完成记录。
- `已完成` 为子智能体接入十枚本地头像：按 App Server 子线程 ID 稳定分配，实时状态、历史完成记录与只读子线程检查器始终显示同一头像，避免并行任务仅靠名称辨识。
- `已完成` 新增独立“智能体”工作台：右侧环境检查器的“子智能体”摘要提供“查看全部”入口，该入口在当前会话右侧工作区平级打开全部子智能体 Tab，不再铺满替换工作区；左侧“智能体”入口保留完整目录。目录按参考的 Codex 桌面信息层级将当前任务子线程分为“已开启”和“完成”两组，每行显示头像、名称和相对时间；点击后进入既有只读详情，不打断主会话草稿。
- `已完成` 将右侧审查与子智能体详情统一为可切换的多 Tab 工作台；桌面布局下扩展栏始终从窗口顶端到底部独立成栏并拥有自己的顶部，会话顶部只属于左侧主任务。空间不足时收窄扩展栏并隐藏环境信息，只有真正窄屏才覆盖会话。窗口最右上角的固定面板图标统一展开或收起工作区并保留全部 Tab 状态。
- `已完成` 将桌面会话内容列限制为最大 820px 并居中布局；剩余空间由环境信息和右侧扩展工作区使用，窄窗口下才允许内容列自然收缩。
- `已完成` 将会话用户消息导航短横线锚定到会话窗口最左侧，避免随居中内容列移动，同时保留悬停渐变和点击定位。
- `已完成` 在发送前校验剪贴板临时图片生命周期；失效文件会被安全移除并提示重新粘贴，避免持久化阶段抛出文件不存在异常。

- `已完成` 对话左侧提供固定在视口中部的 Codex 风格用户消息导航：每条已发送的用户消息只对应一根短横线，未悬停时全部等宽；鼠标移入后当前横线高亮并最长，上下相邻横线按距离逐级变短，移出后恢复等宽；点击横线会精确定位到对应的用户消息。滚动时整列位置不变；助手回复、命令和文件变更不生成横线。
- `已完成` 创建项目不再强制要求主目录：无目录项目可先保存、重启后保持非活动，并可在编辑项目时添加首个源文件夹；补充目录时保留置顶状态，在添加前不会启动任务或隐式授权任何本地目录。
- `已完成` 将 Release、Profile 与 Debug 构建的偏好和历史加密密钥统一保存到应用专用的本地存储文件；升级旧版 Release 时仅尝试一次迁移既有 Keychain 值，之后不再访问 Keychain。Release 与开发构建仍使用不同的 Application Support 目录。该方案适合单用户本地使用，但同一用户目录中的其他进程理论上也可能读取密钥。
- `已完成` 将左侧栏顶部的产品名更新为 17px 粗体 “Xedoc”，并移除其前方绿色星标图标。
- `已完成` 会话时间线未处于最后一条时，在 Composer 上方显示 Codex 风格的居中圆形下箭头；点击以约 260ms 平滑滚到真实末尾并恢复自动跟随，同时避让任务计划与处理提示浮层。滚动结束若与列表布局同帧发生，提示状态会延后至该帧结束安全同步，不会触发布局期重建。
- `已完成` 修复设置中的“编码 > 钩子”：改由 Codex App Server 异步发现项目、用户和插件来源的全部钩子，展示 loading、空和读取错误状态；运行时未启动不会误报为空。信任状态按运行时当前定义哈希判断，不再将过期的 `trusted_hash` 误报为可信；定义修改后由 Codex 重新要求审核。启用、停用、信任和撤销信任走 Codex 的持久化配置接口并在刷新后核验，应用不直接写入 `config.toml`。仓库提供经审核后可启用的 `UserPromptSubmit` 测试钩子，仅将触发时间和事件名写入被忽略的本地日志，不记录提示内容。
- `已完成` 设置页新增“已归档的聊天”界面：从 Codex App Server 刷新当前项目的归档线程，提供搜索、当前项目筛选、项目分组、取消归档、单条永久删除及全部删除确认，并覆盖加载、空列表和错误状态；运行时未就绪或存在运行中任务时删除操作会安全禁用。
- `已完成` 修复助手最终回复出现在创建和命令活动之前的问题：按 App Server `agentMessage.phase` 持久化区分 `commentary` 与 `final_answer`，并修正完成详情的渲染分组；“耗时”作为展开标题固定在该轮任务明细上方，箭头和明细区域以与会话活动清单一致的 180ms 过渡展开或收起，文字随顶部对齐的高度变化淡入，展开后依次显示任务创建、过程说明与命令活动、最终答案和任务完成状态，失败与未知结束状态也保持在末尾，收起时最终答案仍外显。不再按到达位置反复改写控制器条目，流式增量保持在同一消息中；真正发出 `turn/start` 前隔离无法归属的迟到增量和完成事件，并覆盖真实协议 phase、流式合并、启动竞态、完成详情布局与兼容回退测试。
- `已完成` 实现设置中的“配置”页：支持持久化批准策略和后续新任务推理强度，展示由 Codex 配置管理的能力边界，并接入模型、Provider 与字段来源状态查看和 CLI 运行时诊断。
- `已完成` 修复设置“配置”页在窄窗口下的横向溢出；标题操作会换行，受 App Server 管理且无法由应用读取的能力改为准确的只读状态标签。
- `已完成` 设置导航的“返回应用”和“搜索设置”固定在顶部；滚动分类菜单时仍保持可见。
- `已完成` 设置导航中尚未实现的项目会显示“（待开发）”后缀，避免将占位入口误认为可用功能。
- `已完成` 设置中的“插件”改为右侧设置工作区页面，直接管理已安装插件、MCP 服务器和可用技能；切换时刷新三类数据，不再以弹层覆盖设置。
- `已完成` 审查工作区按展开/收起状态重新分配横向布局：展开时固定会话阅读宽度并排列 Inspector 与全高审查列，收起时审查列移除且 Inspector 悬浮回到最右侧；窄窗口保留覆盖式面板。
- `已完成` 成功完成的任务会请求 macOS 本地通知，并在 Dock 显示红点；Dock Tile 会显式开启 AppKit 应用徽标显示，并在系统不绘制 `badgeLabel` 时叠加原生红点图层，确保首次徽标不会被 NSApplication 的默认设置隐藏。失败、停止和未知结束不会触发。时间线滚到末尾只确认应用内提醒；用户显式打开已完成任务，或从 Dock 切回且右侧仍显示该任务会话时，才清除对应 Dock 红点，设置、插件等其他工作区不会清除提醒，其他未查看任务仍保留计数；通知权限被拒绝不会影响任务流程。
- `已完成` 文件变更摘要对 SVG 文件提供悬停图片预览，其他文件维持 Diff 预览行为。
- `已完成` 左侧菜单底部新增设置与帮助入口。点击设置后整个工作区左栏替换为与主侧栏保持相同宽度的设置导航，右侧提供常规权限、工作区目录、默认编辑器、终端位置、主题、运行时、插件、账户和快捷键等功能入口；帮助菜单支持 Chrome 扩展、快捷键和帮助中心，并针对窄窗口调整布局与菜单位置。
- `已完成` 外观设置页提供指针光标、Dock 图标选择、减少动态效果、UI/代码字号、差异标记和字体平滑控件；左侧直接预览 `codex-desk-icon-traced-light.svg` 的深棕渐变底和柔和橙色叶片，右侧预览命令云，二者保持等大；点击后会立即更新 macOS Dock 图标，无需重启应用；切换任务恢复历史时显示无底板的同色叶片加载标记。
- `已完成` 真实任务完成时 Dock 徽标数字对应未查看的完成任务数；原生兜底徽标已放大并加粗数字，适配标准及较小 Dock 尺寸下的可读性。
- `已完成` 从拉取请求、已安排或插件工作区点击左侧任务会立即返回对应会话并恢复任务；任务搜索结果使用相同的返回会话导航。
- `已完成` 收紧会话中已完成活动摘要的视觉层级：标签改用低对比度灰色，收起时右向、展开时下向的箭头以旋转动画过渡；详情区域保持整行宽度并仅沿高度平滑展开和收起。
- `已完成` 按参考截图收紧侧栏项目树的纵向间距：项目节点与普通任务行约 32px 高，任务标题在固定行高的内容区内垂直居中，移除任务行之间多余的空白占位；点击整个项目行可展开或收起任务列表，左侧采用 Codex 风格的打开/关闭文件夹轮廓图标，以不同轮廓清晰表达层级状态，项目与任务背景之间保留细微留白；批量选择行固定为 56px 以容纳复选框所需的点击高度，任务缩进与悬停操作保持不变。
- `已完成` 为项目树展开和收起增加可中断的平滑高度动画；快速反向点击会沿当前进度自然衔接，不产生空白溢出。
- `已完成` 修复应用启动自动连接运行时时左侧任务列表点击无反应的问题：启动阶段允许选择已恢复的本地任务并立即切换缓存视图，App Server 就绪后再完成远端恢复。
- `已完成` 全局移除 Material 点击面的水波纹扩散效果，覆盖按钮、任务行、菜单项及其他继承工作台主题的可点击控件，同时保留悬停、选中和键盘焦点反馈。
- `已完成` 修复项目切换期间排队的历史快照使用可变当前项目 ID，避免 Sourcetree 等项目的消息被写入或显示到另一个项目；保存任务现在固定快照所属工作区键，并覆盖竞态回归测试。
- `已完成` 修复从非当前项目节点的“新建任务”入口创建会话时仍复用当前项目的问题；该入口现在先切换并连接所属项目，再创建空任务。
- `已完成` 修复显式切换项目后恢复已完成当前任务仍保留蓝点的问题；项目切换恢复会确认已结束任务，启动时自动恢复仍保留未查看提醒。
- `已完成` 修复跨项目快速切换时迟到的完成确认污染当前项目的问题；任务确认携带原工作区校验，旧项目蓝点状态不会因新项目操作被覆盖。
- `已完成` 将完成提醒的已查看 ID 纳入非当前项目任务列表缓存，修复任务查看后切回其他项目时蓝点因读取当前项目确认集合而再次出现的问题。
- `已完成` 按参考截图收紧 Codex 工作台的字体、色阶和图标层级：正文 12–13px，侧栏/辅助信息使用分级灰色，Markdown 标题比例同步调整，常规功能图标收紧至约 15–17px；八套兼容预设使用可区分且满足按钮前景对比度的强调色。
- `已完成` 移除已完成任务“耗时”展开按钮的外圈描边，保留文字、展开箭头、圆角点击区和下方分割线。
- `已完成` 收紧会话时间线的分隔语义：只在“已处理 X 秒”和完成后可展开的“耗时”下方保留分割线，其他条目之间只保留原间距。
- `已完成` 新建任务时只保留真正的内置欢迎项；从 App Server 恢复的历史以用户消息开头时，该消息不再被误认为欢迎项并泄漏到空白对话。
- `已完成` 插件设置恢复带确认的卸载入口；MCP 列表按当前项目目录加载并标注用户、项目或托管范围，项目级来源只在 Codex 已信任当前项目时生效，启停只写回实际定义服务器的配置文件，无法识别来源的托管服务器保持只读；MCP 表头、项目信任表头与技能路径兼容 TOML 双引号和单引号形式，启停保留原配置格式且不生成重复技能块；项目切换会使旧 MCP 刷新失效并清空旧列表，扩展配置写入期间禁止切换主项目；插件、MCP、技能及添加服务器失败均在当前界面显示统一错误反馈，配置写入成功后的刷新失败优先显示为警告，即使当前 MCP 标签仍保留列表刷新错误也不会冒充操作失败，添加表单会正常关闭。
- `已完成` 归档成功会立即从侧栏隐藏任务；归档前已经开始的旧刷新响应和归档通知触发的本地 session 回退都不能再把该任务补回活跃列表。
- `已完成` 将归档并发边界收窄到目标任务：同项目其他任务运行时仍可归档空闲任务，运行中的目标任务会显示需先停止的明确原因；项目菜单可进入批量选择，点击归档后立即跳过运行项并反馈数量。归档返回已归档、运行中、处理中和运行时不可用的结构化结果；App Server 确认前任务行显示处理中状态，并锁住当前线程 Composer 的新 turn 提交。
- `已完成` 侧栏任务首次显示后的列表位置保持稳定；运行、停止、完成和服务端更新时间变化只刷新任务数据，不触发重排，新发现任务才会追加到末尾。
- `已完成` 侧栏任务长标题使用固定在任务行末端的右侧透明渐隐裁切而非省略号，保持 Codex 风格的信息密度；完整标题仍可通过悬停读取。
- `已完成` 用户消息气泡保持右侧定位，并对长文本使用左对齐的自然换行；短消息不再因列宽扩展为空白气泡。
- `已完成` 助手回复在会话时间线中直接显示 Markdown 正文，不再于每条回复左上方重复强调“Codex”身份；当前项目内的现有文件链接使用带文件名和图标的紧凑文件行，Markdown 强制换行后的文件行会从下一行左侧对齐，不再相对上一行文字下沉错位；缺失、越界或项目外目标保留普通链接及既有安全限制；工具、审批、错误和系统记录的类型标题仍保留。
- `已完成` 会话中的项目文件行不再以省略号截断长文件名；文件名会优先占用会话的全部可用行宽，仅在超过容器时自然换行并完整显示，点击和项目边界校验语义不变。
- `已完成` 文件行点击、Markdown 预览读取和外部打开前均重新校验当前磁盘目标，阻止渲染后被替换的越界符号链接；普通链接保留嵌套 Markdown 样式和链接图片，链接内本地图片同样限制在当前项目并以 `alt` 提供读屏语义，无可见身份标题的助手回复保留身份语义；异步文件行重排仅在用户仍跟随时间线底部时重新贴底，且会越过 Markdown 表格的横向滚动容器定位纵向时间线。
- `已完成` 兼容旧版 App Server 未附任务 ID 的完成事件：多任务并行时通过权威列表对账当前及后台任务的终态，避免当前任务永久显示为运行中。
- `已完成` 修复 macOS Dock 启动图标在圆角底板外多出无圆角黑色正方形、视觉尺寸偏大，以及 `flutter run -d macos` 回退为 Flutter 默认图标的问题：`AppIcon` 与 Debug 运行时 `DockIcon` 保留同源的圆角深色底板和橙色命令云，圆角外侧使用真实透明像素，并以约 10% 光学安全边距匹配相邻图标；运行时不再自绘 Dock 瓦片。
- `已完成` 校准结绳 Dock 图标的透明安全边距，使其与命令云使用相同约 10% 的光学内缩；两种图标在 Dock 中不再因底板铺满画布而显得一大一小。
- `已完成` macOS Flutter 桌面客户端、Codex App Server stdio 通信、线程创建与任务发送；单个任务失败后连接保持可用，运行时异常退出或启动失败会以 1/2/5 秒有限退避自动恢复，并防止已取消的启动流程回写状态或遗留进程。
- `已完成` 多工作区、窗口尺寸、主题、推理强度、审批模式和线程历史的本地持久化；审批模式在重启后恢复，默认仍为“请求批准”。左侧栏直接展示并切换工作区列表，可新建、保存、切换或移除记录。新建项目使用 Codex 风格弹窗，可选项目名称，源文件夹支持点击选择或拖拽目录；项目具有独立身份与任务归属，新项目从空列表开始，不会载入同目录既有 Codex 会话。每个工作区独立保留主目录与多个附加目录；项目独立历史升级会将原目录索引缓存自动迁移到项目 ID，写入任一项目时会保留其余加密缓存并串行化并发保存，防止切换或重启后任务列表丢失。新建只串行保存非活动项目列表，不改写当前项目的旧版附加目录镜像，保存失败时内存与磁盘边界保持一致；恢复或显式切换后自动连接，附加目录可随时增删且只影响后续新任务。
- `已完成` 已查看的任务完成提醒写入工作区加密历史快照；重启或侧栏重建不会重复显示，同一任务再次成功完成时会清除旧的已查看状态。当前已打开的任务成功完成时若时间线已经处于底部会自动确认；停留在上方时保留“任务已就绪”和小蓝点，用户滚到底部或点击对应任务行后清除。停止、取消和无法识别的结束状态保持已查看，不产生成功蓝点；鼠标移入时蓝点暂时隐藏并让位给快捷操作，切换或新建项目不会继承上一项目的已查看 ID，当前任务的滚动状态也不会误确认后台完成任务。
- `已完成` 用户消息气泡在鼠标悬停时于下方显示该条消息的本地时间，移出后收起；悬停状态仅属于单个气泡，时间作为不参与布局的悬浮层显示，不推动后续消息。
- `已完成` 项目“编辑”入口对齐 Codex 桌面端：项目名称可编辑并持久化，名称输入框保持单层外框，主/附加源文件夹以分组卡片展示，可添加或移除附加文件夹，并支持移除当前本地项目记录而不删除磁盘目录。
- `已完成` API Key / 浏览器登录、Codex 原生 Provider 配置接入和运行时诊断脱敏；应用不再维护独立中转站配置，并通过 App Server 只读展示当前项目最终生效的模型、Provider 与字段来源。
- `已完成` 从 App Server 模型目录切换后续新任务的模型，并按模型能力联动推理强度；模型气泡不显示额外图标，跟随配置简化显示为“默认”，其他模型只显示展示名称并隐藏内部模型 ID；模型和推理强度控件不绘制额外边框，推理强度仅显示“默认”“高”“中”“低”等名称，且不覆盖历史任务模型。
- `已完成` 对话历史恢复、线程搜索、置顶、归档、本地历史导入导出和 App Server 分页补齐；恢复时以 App Server 命令项为准，并按命令项 ID 补回服务端遗漏的本地已完成命令。当前项目的普通任务行使用 4px 外边距、30px 树形文字缩进和 32px 高度，行尾运行指示器为 10px；悬停显示置顶、归档快捷图标，右键打开完整任务菜单，替代常驻三点入口。任务搜索改为项目列顶部图标触发的 Codex 风格弹层，支持跨已加载项目按标题、预览、项目或 Provider 查找并恢复任务，且提供新聊天、打开文件夹和文件变更搜索快捷操作。任务运行中可从搜索结果切换同项目或其他项目任务，原任务继续在后台执行。若另一 Codex 客户端仍持有任务 writer，恢复或归档会显示可重试的非阻塞提示，不污染当前时间线；归档重试会包括冲突项及批量操作中其后尚未处理的任务。若任务已在其他客户端归档，恢复会要求先取消归档，任务回到活跃列表后才可重新打开。提示与操作严格绑定原项目，切换工作区即清除。
- `已完成` writer 冲突重试在运行时暂不可用或工作区切换期间不会清除提示，避免操作未执行却失去恢复入口。
- `已完成` 切换任务首次加载时在当前会话窗口内显示 Codex 风格历史加载页，并在惰性列表的实际范围稳定后定位到最后一条记录再显示，侧栏、会话顶部栏和右侧工作区保持可用；覆盖应用重启后的首次打开与内容高度不一的长会话；缓存最近 8 个任务的完整时间线页面和滚动位置，再次切换直接显示已保活页面。加载与校准状态按任务视口隔离，另一个长会话仍在校准时快速切回，也不会重置保活页面的阅读位置。任务切换与实时回复的自动定位都不播放平滑滚动或重新定位动作；文本选择不创建嵌套滚动区，滚轮始终驱动整条时间线；超出上限的较早任务重新读取历史；历史读取失败不会显示前一任务内容。
- `已完成` 时间线仅在用户停留于底部附近时自动跟随新内容；主动上滑后会保留阅读位置，正常内容重排与自动滚底不会被误判为手动阅读并中断后续跟随，已排队的下一帧滚底回调也会重新确认跟随状态，完成态的普通控制器刷新不会再把列表拉回底部。当前任务完成时若已在底部会自动确认完成提醒；停留在上方则保留提醒，直至用户滚到底部，或窗口扩大使底部自然进入可见范围。完成提示出现和收起都不改变 Composer 实际高度或时间线底部留白。
- `已完成` 会话输入区悬浮覆盖在完整高度的时间线之上，滚动内容可经过输入框后方，并在接近输入区时通过透明渐变平滑隐去；输入框作为上层实体轻微覆盖渐变底端，而不是挤压时间线末尾，输入区及恢复提示动态变高时仍会同步调整尾部留白，并额外保留 52px 底部安全间距，避免末尾状态或文件统计贴住输入区。无明确实时活动时使用固定悬浮在 Composer 上方的圆形三点 loading，不作为时间线条目参与滚动；三个点按错开的相位呈波浪状跳动，并遵循系统减少动态效果设置。Composer 不再重复显示“正在处理任务”状态胶囊。
- `已完成` 随任务发送的本地图片会在用户消息气泡中显示缩略图，并随时间线缓存图片路径；点击会话图片可复用 Composer 图片相同的 Codex 风格沉浸式预览、缩放、默认应用打开和另存副本操作。
- `已完成` 带图片的“调整方向”在 App Server 接受响应与任务完成竞态下仍会写入当前会话并保留图片引用；接受响应晚于完成通知时，方向仍按发送时机排列在完成记录之前，等待确认期间临时图片不会被提前清理。若用户在响应返回前切走再切回同一任务，旧响应会识别会话视图已重载并对权威历史去重，不再使用切换前的时间线索引重复或错序插入。
- `已完成` 左侧导航按 Codex 风格整理为项目树：工作区使用文件夹节点，任务作为各项目下的文件缩进显示；项目树按视口惰性构建，任务量较大时状态刷新不会重新布局所有会话行。启动时预读全部项目的本地任务缓存并默认展开，点击整个项目行即可在当前会话中展开或收起，左侧文件夹图标以打开/关闭状态表达层级，切换项目不会收起其他项目。其他项目任务为只读预览，打开时会先切换到所属项目，不允许在当前项目中继续、置顶、归档、重命名或删除。置顶任务独立分组，选中任务使用低对比圆角背景，每个运行中的任务在行尾显示高对比加载指示器，当前任务可点击保持焦点；选中态与 loading 仅属于当前项目，其他项目的缓存任务不会继承。刚创建但尚未进入服务端列表的任务会先显示在侧栏，即使用户立即新建或打开另一项任务，也会保留所有仍在启动的后台任务占位行，未查看的已完成任务显示蓝色完成提醒且打开后自动清除，错误任务显示红色叹号；悬停项目自动展示包含完整路径的详情卡片，项目行不再额外弹出路径 tooltip；详情卡片支持持久化项目置顶并读取当前项目或本地缓存任务数，更多按钮可打开置顶、编辑、工作树、归档和移除菜单；运行中仍可新建任务、切换至同项目任务或打开其他项目任务，旧任务通过共享 App Server 按项目隔离地继续执行，完成状态和提醒写回原项目；打开运行中后台任务会通过只读历史加载，不重复申请 App Server writer，且保留 `turn/steer` 调整方向能力。旧版运行时缺少任务 ID 的完成通知会通过权威任务列表对账清除后台状态，后台审批面板会标明所属任务；运行期间仍不能重连运行时或修改插件。
- `已完成` 将跨项目切换串行化，连续快速选择多个项目时，旧历史恢复、运行时刷新和配置保存不会迟到覆盖最终项目；同一非当前项目的多个完成事件按项目队列执行读取—合并—保存，无任务 ID 的兼容完成通知会查询全部相关项目的权威任务状态，不再留下永久运行标记或漏掉蓝点。
- `已完成` 接入 `turn/plan/updated` 实时计划通知，以悬浮步骤面板展示待执行、进行中、已完成状态和当前步骤位置；三点 loading 与计划面板共享底部自适应浮层，按面板实际内容保持固定间距，短计划和小窗口下不会漂移或越界；任务结束后自动收起。
- `已完成` 隐藏 App Server 命令输出增量等协议级执行事件，避免它们作为“执行事件”重复占据对话时间线；审批、任务结果和恢复历史中的实际命令记录继续展示。
- `已完成` 连续的历史命令、文件读取和工具调用以 Codex 风格活动清单归组显示；连续命令之间的“已自动批准本次操作”不会切断活动组，清单默认折叠、可点击展开或收起，并保留每项的完整事件详情提示。
- `已完成` 对话时间线接入 App Server 的 `item/started` / `item/completed` 生命周期：单一且明确的结构化命令动作会区分文件读取、项目内搜索、目录列出和文件编辑，并分别显示查询词与目标目录；混合文件操作与构建、测试的复合命令回退显示完整命令，不会被首个文件动作掩盖。推理摘要通过 `item/reasoning/summaryTextDelta` 流式显示 `Planning…`、`Clarifying…`、`Inspecting…` 等具体内容，高频摘要增量会合并通知以避免逐 token 重建，最终文本仍完整；摘要严格绑定当前推理项，迟到更新不会覆盖后续活动，无摘要时回退为“正在分析”。计划整理、网页搜索/打开/页内查找、MCP/动态/协作工具、图片处理、等待、上下文压缩、审查和回复也会在服务端明确报告时显示对应实时动作，动态技能读取会进一步显示技能名。技能读取完成后在会话中保留“已读取…技能”，子代理使用独立状态徽标并按子线程 ID 从“已开始工作”原位更新为完成、失败或停止；两类记录均写入加密历史并可由 App Server 历史恢复。子代理徽标可打开只读子线程检查器，宽屏替换右侧环境栏、窄屏以覆盖层展示详情页并保留 Composer 的文字、附件、Skill、目标和计划模式，嵌套子智能体可继续点击查看；检查器分页读取真实子线程的任务说明、状态、分析、命令与 Markdown 结果，并且不调用 `thread/resume` 争抢 writer；运行时未连接时显示可重试错误。子线程完整历史使用最近 8 项 LRU，工作区切换会取消旧刷新、清空缓存并拒绝迟到结果；运行时停止、重启或意外退出同样会结束在途 loading、保留已有内容、把旧 working 状态标为停止并拒绝旧连接结果回写。异步刷新、错误重试、宽窄窗口断点、草稿保留、缓存淘汰、工作区隔离、运行时连接隔离、嵌套导航和关闭生命周期均有测试覆盖。其他实时活动完成后自动清除，未知的新操作类型使用中性活动提示，避免误显示为长时间“正在思考”。命令完成后归入既有活动清单，上下文压缩随服务端历史恢复；仅当没有活动事件时显示 Codex 风格低干扰“正在思考”行。每个结束的 turn 使用服务端耗时（缺失时安全回退）显示“耗时”记录，并随本地历史恢复。来自此前已恢复任务的后台或迟到生命周期事件不会污染当前任务的时间线、命令、计划、文件变更或耗时；切换任务后迟到的停止请求结果与无任务 ID 的旧版完成通知也只会更新原任务状态。
- `已完成` 已识别的实时活动状态与运行中命令复用“正在思考”的扫光节奏，并遵循系统减少动态效果设置。
- `已完成` 运行中的任务在本轮用户消息之后固定显示按秒更新的“已处理”时长，流式回复、文件操作和实时活动继续显示在其下方，计时器不会随最新活动移动到时间线底部；暂停或调度延迟后按真实起始时间纠偏，该可查询的辅助技术语义不会逐秒触发播报。结束后将推理、状态、命令和工具等执行过程归入可展开或收起的“耗时”标题；“耗时”内的命令和工具活动摘要默认折叠，只有显式点击后才展开明细。无执行明细时仅显示非交互耗时，最终回复、审批和错误始终外显并保持原始时间顺序。
- `已完成` 修复时间线在接近末尾时向上滚动被延迟自动贴底回调反复拉回的问题：用户一旦离开最后一项即保留阅读位置；完成提醒仍会在真正滚回底部或窗口扩大自然露出末尾时正常确认。
- `已完成` Composer 上方悬浮的文件统计和活动提示不再接收指针事件；滚轮和触控板经过该区域时继续滚动下面的时间线，不会产生受阻或回弹感。
- `已完成` 稳定可变高度时间线的滚动范围：常见会话会预先布局 Markdown、活动清单和文件摘要，避免惰性子项在慢速滚动进入缓存区时改写总高度、放大滚动条拇指并抵消用户位移；超大历史仍使用有界惰性缓存。
- `已完成` MCP 表单枚举值校验：拒绝重复枚举项及不属于枚举的默认值，避免异常 schema 触发表单构建错误。
- `已完成` 接入 App Server 的 `mcpServer/elicitation/request`：支持受限标量字段的结构化表单和 URL 确认，表单输入须由用户显式提交，URL 不自动打开，自动审批模式不会代替用户确认；取消、拒绝和不支持的 schema 均安全结束请求，后台任务会标注归属。
- `已完成` 命令、文件变更与额外权限审批改为贴近 Composer 的 Codex 悬浮卡片，显示原因与命令块；命令和额外权限支持“允许一次”、拒绝及下拉菜单中的“允许类似操作”，文件变更仅发送单轮允许，分别映射为协议支持的 App Server 决策。
- `已完成` Composer 模型选择器左侧增加背景信息窗口指示器和 Codex 风格使用量弹层，展示对话上下文本地估算百分比、已用/上限标记数，并在接近上限时提示压缩较早内容或开启新任务；估算覆盖时间线、草稿、附件路径和技能描述，服务端未提供精确统计时明确标注“估算”。
- `已完成` Codex 回复按 GitHub Flavored Markdown 渲染，支持正文选择复制及标题、列表、加粗、链接、引用、表格和代码块；HTTP、HTTPS 与邮件链接交由系统应用打开，当前项目内的 `.md` / `.markdown` 链接使用覆盖工作台的格式化阅读器，支持预览/源码切换、相对文档导航、本地图片、返回、外部打开和 `Esc` 关闭，并在源码模式定位 `文件:行号[:列号]`；其他项目内文件仍交由系统默认应用打开。所有本地路径会解析符号链接并限制在项目目录内，单份 Markdown 限制为 8 MB，源码模式对超过 4096 个字符的单行进行带长度提示的截断渲染，且不自动加载远程图片；用户输入、命令与系统记录维持纯文本，避免将日志内容误解析为 Markdown。
- `已完成` 支持在运行中任务的 Composer 中连续暂存多条待发送方向：每次按 Enter 生成一条非持久化临时项，并按顺序堆叠在 Composer 顶部限高滚动栏，不会挤出输入框或提前写入对话记录。每条都可独立经 App Server `turn/steer` 发送或删除，方向发送严格串行；未手动发送时，任务每轮结束会按队列顺序自动发送下一条，自动续发失败会恢复队列并回滚乐观用户消息，手动发送恰逢任务结束且 steering 失败时也会安全转为下一轮。空 Composer 不会生成无意义的待发送项，切换任务后迟到的发送结果也不会污染新任务。运行中仍可附加文件、图片、文件夹、技能、目标和计划上下文，停止请求会携带当前 thread 与 turn ID，且仅在活动 turn 已知时开放。
- `已完成` 接入 Riverpod 管理应用共享控制器的所有权与订阅；保留 `CodexController` 的 `ChangeNotifier` 桥接以兼容既有业务状态与测试注入。
- `已完成` 将主题偏好纳入独立 Riverpod Provider，并统一工作区管理、账户、运行时、归档、文件审查、插件、marketplace 与 Git 弹窗的共享控制器订阅；测试和嵌入场景仍可显式注入控制器。
- `已完成` 插件 marketplace 管理、启用/停用配置的原子写入，以及 Git 状态、Diff 和显式提交工作流视图；插件命令复用自动发现的 CLI 绝对路径，不受桌面应用 `PATH` 限制，路径解析和命令调用均有 20 秒超时保护。插件目录读取官方 manifest 的展示名、简介、分类和 PNG/JPEG/SVG logo，并按分类呈现；已安装插件只在“已安装”区域出现，不会重复出现在精选或分类目录中，Logo 路径限制在插件源目录内；缺少或无法读取 logo 时使用 Codex 风格旋涡标记，左侧“插件”入口也复用该标记。左侧菜单项固定 32px 高，图标与文字在行内垂直居中。
- `已完成` 输入区“添加”菜单、桌面拖拽与剪贴板附件：支持文件/文件夹与项目上下文、线程目标、计划模式、技能录制和动态技能选择；文件可拖入，也可从 Finder 复制后在输入框粘贴，剪贴板屏幕截图会作为临时 PNG 图片附件发送。临时图片由会话控制器统一记录 Composer 保留计数及当前/缓存时间线、排队方向、运行中提交、失败重试引用；任务完成、切换功能页、Composer 重建或控制器切换均不会提前删除，最后引用消失时即使 Composer 未挂载也会主动清理，控制器销毁时兜底清理；图片附件支持缩略图和覆盖工作台的 Codex 风格沉浸式预览，提供按原始像素计算的真实比例、保留查看中心的手势/按钮/双击/键盘缩放、默认应用打开、另存副本与 `Esc` 关闭，附件自动去重，普通文本粘贴仍保持原有行为；并通过 `localImage`、`thread/goal/set`、`collaborationMode` 与结构化 `skill` 输入接入 App Server。
- `已完成` 右侧检查器对齐 Codex 桌面端的环境信息卡：展示本地变更、分支、提交/推送、创建拉取请求和比较分支入口；标题、操作行、分组和文件行采用 18/14/14/13px 紧凑字号；Git 视图支持文件级暂存、确认后还原、显式提交/推送及通过本机 GitHub CLI 创建 PR。
- `已完成` 左侧任务栏与右侧环境栏支持通过 Codex 风格的细分隔条拖拽调整宽度，并在窄窗口保留右侧自动收起行为。
- `已完成` 左侧菜单采用独立的 Codex 深色背景表面；侧栏分隔条拖拽后的宽度会持久化到本地 UI 偏好，并在下次启动恢复。
- `已完成` 会话窗口右侧内嵌环境信息面板并隐藏任务文件明细列表，保留变更操作入口。
- `已完成` 展开右侧审查或子智能体工作区时自动隐藏环境信息面板，收起后恢复。
- `已完成` 将工作区改为左右两列主结构：项目列和工作台列各自拥有独立顶部栏；环境信息在宽屏下保留为工作台内部的辅助检查区。
- `已完成` 将任务名称、Provider / App Server、沙箱边界与文件变更入口收拢到工作台顶部；当前任务名称支持点击后行内编辑，Enter 保存、Esc 取消。
- `已完成` 统一 tooltip、项目详情与文件 Diff 悬停预览的 450ms 显示延迟，快速掠过控件或列表行不会弹出临时浮层。
- `已完成` 隔离测试运行时配置，避免测试将 macOS 系统临时目录写入开发版工作区偏好；应用会拒绝该目录作为项目或附加目录，并在恢复时自动清理旧记录。
- `已完成` 收紧侧栏任务选中态的垂直内边距，并以顶部图标打开的搜索聊天面板替代常驻搜索框，保持项目树扫描密度。
- `已完成` 收紧侧栏项目详情卡片的字号：项目名、任务数/路径与“编辑项目”入口分别采用 16/14/14px，保持悬停卡片的信息密度。

## 待办

### P0：发布与可靠性

- `已完成` 建立未签名 Release 打包流程：`build_dmg.sh` 会按 `pubspec.yaml` 版本生成 DMG，发布说明见 `RELEASE_NOTES.md`；2026-08-21 已生成并通过 `hdiutil verify` 校验。
- `待办` 在干净 macOS 用户账户或测试机完成 DMG 安装回归；步骤见 `RELEASE_CHECKLIST.md`，需要人工操作安装后的应用。
- `受阻` 完成首次 Developer ID 签名和 notarization 提交。`build_dmg.sh` 已支持签名应用与 DMG、`notarytool` 提交、staple 和 Gatekeeper 评估；仍需提供 Apple Developer 证书、Keychain notary profile 与发布权限，未具备前只能生成未签名 DMG。
- `已完成` 为安装脚本增加“仅构建、不安装”和“仅安装、不构建”参数；正常退出请求被取消时会对目标应用进程回退使用 `SIGTERM`，且仅在 `/Applications` 不可写时请求管理员权限。
- `已完成` 增加 CLI 不可用的启动失败、可复制脱敏诊断和重试回归测试。

### P1：核心体验

- `已完成` 按 [IN_APP_BROWSER_DEVELOPMENT.md](IN_APP_BROWSER_DEVELOPMENT.md) 完成 macOS `WKWebView` 阶段 0 技术验证，并保留独立浏览器工作区承载层；设置入口只展示智能体调用能力边界，不手动创建 WebView。
- `进行中` 按 [IN_APP_BROWSER_DEVELOPMENT.md](IN_APP_BROWSER_DEVELOPMENT.md) 完成智能体按需唤起适配：设置提供持久化总开关，只有可回复的 `browser/open`、`browser/navigate` 请求才显示 Codex 风格底部权限卡片，并在用户允许后打开内置浏览器工作区加载具有主机名的 HTTP/HTTPS 页面；嵌套请求地址会在卡片中完整显示，相同 URL 的每次已批准导航都会重新执行，关闭开关会拒绝并清理等待中的浏览器审批。`browser` / `computer-use` 活动和无 ID 通知不会绕过确认自行导航。完整公开协议接入、链接打开位置偏好、独立浏览数据/历史、下载确认与受控下载路径仍待交付。不共享 Chrome/Safari 登录态，不自动向 Codex 发送页面数据，浏览器受控能力另行进行协议与安全评审。
- `进行中` 浏览器实际导航前会解析域名并拒绝解析到回环、私网、链路本地或多播地址的主机；重定向继续经过同一策略复核。
- `已完成` 用户消息气泡保持右侧对齐，内部单行与换行文本维持自然左对齐；悬停时在时间旁提供复制和修改入口，修改会原位展开 Codex 风格内联编辑器，支持取消和发送，修订内容作为下一轮任务提交（运行中则排入调整方向），不修改既有会话审计记录。
- `已完成` 收紧“已运行命令”等活动摘要与展开明细的上下间距，减少时间线活动清单的空白。
- `已完成` Composer 支持 Codex 风格 `/` 浮层：输入空 `/` 会先显示全部快捷指令，随后显示当前项目已启用技能的名称、简介及个人/项目/系统范围；继续输入文字会持续筛选快捷指令与技能而不关闭浮层。鼠标、↑↓、Enter/Tab 可选择，键盘移动会自动将高亮项滚入可视区，技能会以紧凑蓝色 Codex 徽标作为结构化上下文加入输入区；点击徽标可查看说明、范围、来源路径与本地 `SKILL.md` 内容，读取失败会显示明确反馈，关闭按钮仍可移除。Esc 关闭浮层。选择“目标”进入带底部标签的内联目标输入，并将文字作为本次任务目标提交；选择“MCP”保留 `/` 并展示当前配置服务器的认证能力与启用状态，未知状态不会误报不支持；面板中的 Enter 只会关闭 MCP 视图而不会发送 `/`，读取失败时保留的缓存行会标注为旧结果；选择“代码审查”保留 `/` 并打开紧凑的内联范围菜单，Enter 会选择未提交更改并作为当前 Git 工作区的审查任务发送；当前任务尚未接收方向时显示等待状态，准备就绪后自动排队，列出的本地/远程基础分支会预填待发送的对比审查任务；IDE 上下文、文件/文件夹、计划模式、录制技能和新聊天直接映射到现有交互。
- `待办` 按 [LOCAL_WORKTREE_DEVELOPMENT.md](LOCAL_WORKTREE_DEVELOPMENT.md) 实现 Codex 风格本地工作树：新任务可在原始本地 checkout 或任务专属 detached worktree 中运行，线程、App Server cwd、审查和 Git 操作绑定同一执行目录；通过可恢复事务安全携带当前本地改动和 `.worktreeinclude` 环境文件，清理后仍保留聊天与快照恢复入口，并以应用外权威所有权记录保护删除边界；支持在工作树创建分支和默认最近 15 个托管工作树清理。基础创建和任务绑定稳定后，再独立交付基于双端检查点的增量 Local ↔ Worktree Handoff、永久工作树、可配置根目录及已安排任务隔离。
- `已完成` 对齐 Codex 的网络等待与失败轮次恢复：App Server 发出 `error(willRetry: true)` 时，在严格匹配 thread/turn 的对应时间线中逐次保留灰色 Wi-Fi 活动行 `Reconnecting... waiting for network`；当前任务直接显示，后台任务按 thread 暂存并在打开时恢复，不污染当前会话。任务保持运行并由服务端自动续接，出现后续进展或结束后等待记录转为非实时历史语义；迟到通知与 `willRetry: false` 被忽略。只有最终返回 `turn/completed(status: failed)` 后才在输入区上方显示行内错误和“重试”，并在同一线程原样重发文字、Skill/图片输入、目标、计划模式及模型设置；重试期间防止重复提交，再次失败后保留入口。停止/取消、缺少原始内存提交的历史失败和其他任务均不会显示或误用该操作，异步结果也不会污染切换后的任务。
- `已完成` 当历史任务被另一个 Codex 客户端持有 writer 时，以输入区上方的非阻塞“已在另一个应用中打开”提示说明状态，并提供关闭另一处会话后的重试操作，不再弹出对话框。
- `已完成` 将项目栏顶部的紧凑图标替换为“新对话、拉取请求、已安排、插件”文字入口，并让后三项打开 Codex 风格的独立右侧工作区；所有用户触发的新建任务入口统一返回会话工作台，包括侧栏“新对话”、项目行“新建任务”和搜索面板“新聊天”。已安排任务会在桌面应用运行期间按本地持久化的时间发送到对应项目，避免抢占正在运行的任务。插件页提供“插件 / 技能”切换、真实 App Server 技能列表和右上角添加菜单；设置按钮会打开带真实数量、分栏搜索与启停开关的“插件 / MCP / 技能”管理弹层，MCP 列表来自 Codex CLI 并支持添加 HTTP 服务器，插件、MCP 和技能配置变更均安全重连运行时。创建插件预填 `$plugin-creator`，录制技能会在新对话中启用结构化技能录制上下文，marketplace 表单支持 GitHub、Git URL 和本地目录，Git 引用仍由 CLI 默认设置解析；PR 页面保留 GitHub CLI 工作流。
- `已完成` 项目栏“新对话、拉取请求、已安排、插件”入口统一使用 14px 辅助导航字号，保留现有点击面、图标与键盘/语义操作。
- `已完成` 修复已安排任务在等待项目切换或运行时重连期间被取消后仍可能发送的问题；取消现在会使该后台调度失效，不会创建新对话。
- `已完成` 修复新建任务或切换历史任务后，旧任务迟到的生命周期和计划通知可能写入当前视图的问题；项目详情卡片的任务数每次打开都会从本地缓存刷新。
- `已完成` 修复文件变更统计将仅含 Diff 头、二进制或元数据的不可计数变更误报为 `+0 -0`；这些情况统一显示未知统计。
- `已完成` 修复任务切换与异步列表刷新交错时，已打开任务重新显示旧的完成提醒；打开任务会立即确认提醒。
- `已完成` 增加线程永久删除与批量归档操作；永久删除会提示 App Server 派生线程影响并移除本地缓存引用，归档无需二次确认且仍可恢复。
- `已完成` 增加 Diff 文件状态筛选、路径搜索与超大预览截断提示；Git 状态与 Diff 保持只读，文件级暂存、确认后的还原、提交、推送与 PR 创建只会在用户显式触发后执行。
- `已完成` 按 [CODE_REVIEW_INTERFACE_DEVELOPMENT.md](CODE_REVIEW_INTERFACE_DEVELOPMENT.md) 将模态审查升级为 Codex 风格工作台审查界面：宽窗口与会话并排且可拖动调宽，窄窗口以保持会话生命周期的覆盖页显示；“最新一轮”和“Git 工作区”共用连续多文件 Diff、结构化行号/Hunk、未修改行提示、固定行号栏和画布级横向滚动；目录树支持筛选、折叠、点击跳转和正文滚动联动。Git 工作区以最多 6 路并发分批加载全部文件 Diff，并接入全局互斥且带失败反馈的暂存、确认还原、提交/推送和 PR 操作；复合 patch 行号重置、布局断点、极窄工具栏、宽屏 `Esc`、长文件树定位、草稿/审查状态保留、滚动折叠数量及 Git 操作进度均有回归测试。
- `已完成` 文件变更不再逐条显示在会话中，每轮仅以该轮的摘要卡片、悬停 Diff 预览和只读审查入口呈现；新一轮开始时清空上一轮摘要，启动失败时恢复，历史恢复只采用最后一轮文件集合。“审核”左侧提供 Codex 风格“撤销”，仅在完整、未截断的任务 Diff 可用且项目没有前台或后台任务运行时，由 Git 核对补丁路径、摘要文件列表、暂存区和当前工作树后反向应用原始 Diff；执行中禁止发送任务和重复提交，暂存改动、路径不一致、冲突或后续编辑都会保留文件与摘要而不回退整个工作区；预览会在窄窗口中自动限制尺寸并选择可见方向，显示真实 hunk 行号，且会跟随异步补齐的文件 Diff 更新。
- `已完成` 收紧文件变更摘要卡片的排版：标题采用 14px 半粗体，新增/删除行统计采用 13px 字号，保持对话时间线的信息层级。
- `已完成` 收紧 Composer 默认输入文字至 14px，并将发送与运行中任务的停止按钮统一为 36px 圆形点击面；上箭头和停止图标保持居中，并保留各自语义样式。
- `已完成` Composer 的 Enter 发送会识别输入法未确认的组合文字：拼音等组合态下，Enter 继续交由输入法取消或确认候选；即使平台先清除组合范围再派发候选确认的 Enter，也不会误提交消息，后续 Enter 可正常发送。
- `已完成` 在任务输入框顶缘以悬浮提示显示文件变更统计，显示文件数量及新增/删除行数且不挤占输入区；Diff 不可用时保留未知统计语义。
- `已完成` 增加插件操作的具体进度、失败原因与“重启运行时后生效”状态反馈；成功重启后会更新提示。
- `已完成` 为运行时诊断提供导出到本地文本文件的入口，并在导出前再次执行凭据脱敏。
- `已完成` 创建项目弹窗选定首个源文件夹后可继续添加多个目录；首个目录作为主目录，其余目录随项目保存为附加目录，并可在创建前逐项移除。

### P2：质量与可维护性

- `已完成` 修复流式输出及迟到条目前插导致既有会话文字反复重排的抖动：时间线条目使用可持久化稳定 ID，惰性列表在顺序变化后按 ID 迁移原 State；未变化消息保留已构建的 Markdown 子树，活动和“耗时”折叠状态不重置。当前回复在 item 完成前使用固定行高的只追加文本布局；内联链接目标闭合前不显示临时标签，闭合后本地链接直接采用最终文件名，隐藏的绝对路径不参与换行，同时保留代码与转义原文。第一段文字前仍显示“正在撰写回复”，正文出现后不再额外占用该状态行，避免 item 完成时整体位移。包含本地文件链接时，item 完成后继续保持该布局，待整批链接去重校验完成再一次性显示最终 Markdown 和文件行；文件行只向下展开并保持回复顶部锚点，不因重新贴底把已显示文字向上推。异常链接降级为普通链接，不阻塞整条回复。
- `已完成` 收紧共享与异步状态边界：项目悬停卡的任务数迁入自动释放的 Riverpod `FutureProvider`；插件、marketplace、Git 摘要/Diff 与 Codex 配置刷新将请求代次作为状态边界，较早结果不再覆盖较新状态；嵌入/测试替换控制器时同步迁移监听与动作目标。
- `已完成` 解耦新建与切换项目：新建在运行时启动、任一前台/后台任务运行或插件配置保存期间仍可执行，只保存为非活动项目并建立空任务边界，不切换工作区、不停止任务或重连 App Server；仅切换项目继续在这些状态下锁定并展示对应原因。
- `已完成` 为历史缓存、插件配置与操作反馈、Git 解析/筛选/大 Diff 和脱敏规则补充分层单元测试及异常路径测试；同时覆盖运行时失败重试、注入控制器重建、线程删除事件与批量归档部分失败/重复提交。
- `已完成` 建立 macOS GitHub Actions CI：格式检查、`flutter analyze`、`flutter test` 和 macOS Debug 构建；主题 Token 已迁入项目内维护，不再依赖仓库外本机路径或外部 UI Kit。
- `进行中` 完善键盘导航、焦点顺序、语义标签和高对比度主题；本阶段新增操作均有文字标签或 tooltip，完整无障碍审计待后续集中执行。
- `已完成` 评估 Windows / Linux 平台支持；结论和启动前置条件见 `PLATFORM_SUPPORT.md`，在 macOS 发布前不启动实现。

## 下一项建议

优先在干净 macOS 用户账户或测试机执行 DMG 安装回归；取得 Apple Developer 凭据后，再完成首次签名、公证与 notarized DMG 回归。
