# Codex Desk 路线图

本文档是项目唯一的后续任务清单。状态约定：`已完成`、`进行中`、`待办`、`受阻`。每完成一个任务，应同步更新本文档和 README。

## 已完成基线

- `已完成` macOS Flutter 桌面客户端、Codex App Server stdio 通信、线程创建与任务发送。
- `已完成` 本地项目、窗口尺寸、主题、推理强度、审批模式和线程历史的本地持久化。
- `已完成` API Key / 浏览器登录、中转站配置、Keychain 凭据保存和运行时诊断脱敏。
- `已完成` 对话历史恢复、线程搜索、置顶、归档、本地历史导入导出和 App Server 分页补齐。
- `已完成` 插件 marketplace 管理、启用/停用配置的原子写入，以及只读 Git 状态和 Diff 视图。

## 待办

### P0：发布与可靠性

- `已完成` 建立未签名 Release 打包流程：`build_dmg.sh` 会按 `pubspec.yaml` 版本生成 DMG，发布说明见 `RELEASE_NOTES.md`；2026-08-21 已生成并通过 `hdiutil verify` 校验。
- `待办` 在干净 macOS 用户账户或测试机完成 DMG 安装回归；步骤见 `RELEASE_CHECKLIST.md`，需要人工操作安装后的应用。
- `受阻` 配置 Developer ID 签名和 notarization。需要提供 Apple Developer 证书、Team ID 与发布权限；未具备前只能使用本地 ad-hoc 构建。
- `已完成` 为安装脚本增加“仅构建、不安装”和“仅安装、不构建”参数，并补充对应使用说明。
- `已完成` 增加 CLI 不可用的启动失败、可复制脱敏诊断和重试回归测试。

### P1：核心体验

- `已完成` 增加线程永久删除与批量归档确认操作；删除会提示 App Server 派生线程影响并移除本地缓存引用，归档后仍可恢复。
- `待办` 增加 Diff 文件筛选、搜索与大文件预览提示，持续保持 Git 视图只读。
- `待办` 增加插件操作的进度、失败原因与“重启运行时后生效”状态反馈。
- `已完成` 为运行时诊断提供导出到本地文本文件的入口，并在导出前再次执行凭据脱敏。

### P2：质量与可维护性

- `进行中` 为历史缓存、插件配置、Git 解析与脱敏规则补充分层单元测试和异常路径测试；本阶段已覆盖插件首次原子写入、Git 未跟踪文件配置、Basic 凭据、运行时失败重试，以及线程删除事件与批量归档部分失败。
- `受阻` 建立 CI：格式检查、`flutter analyze`、`flutter test` 和 macOS Debug 构建。当前 `yeknom_ui_kit` 采用仓库外 path dependency，需先提供可供 CI 获取的 Git 来源或发布包。
- `进行中` 完善键盘导航、焦点顺序、语义标签和高对比度主题；本阶段新增操作均有文字标签或 tooltip，完整无障碍审计待后续集中执行。
- `已完成` 评估 Windows / Linux 平台支持；结论和启动前置条件见 `PLATFORM_SUPPORT.md`，在 macOS 发布前不启动实现。

## 下一项建议

优先执行“Release 打包流程”。它能把当前可用的开发构建变成可复现的安装包，同时不需要访问用户项目或修改运行时行为。
