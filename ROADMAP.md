# Codex Desk 路线图

本文档是项目唯一的后续任务清单。状态约定：`已完成`、`进行中`、`待办`、`受阻`。每完成一个任务，应同步更新本文档和 README。

## 已完成基线

- `已完成` 侧栏任务首次显示后的列表位置保持稳定；运行、停止、完成和服务端更新时间变化只刷新任务数据，不触发重排，新发现任务才会追加到末尾。
- `已完成` 侧栏任务长标题使用固定在任务行末端的右侧透明渐隐裁切而非省略号，保持 Codex 风格的信息密度；完整标题仍可通过悬停读取。
- `已完成` 用户消息气泡保持右侧定位，并对长文本使用左对齐的自然换行；短消息不再因列宽扩展为空白气泡。
- `已完成` 兼容旧版 App Server 未附任务 ID 的完成事件：多任务并行时通过权威列表对账当前及后台任务的终态，避免当前任务永久显示为运行中。
- `已完成` 修复直接通过 `flutter run -d macos` 启动时 Dock 可能显示 Flutter 默认图标的问题；应用完成启动后会显式采用 bundle 已编译的 `AppIcon`，并保留透明安全边距以匹配相邻 Dock 图标的视觉尺寸。
- `已完成` macOS Flutter 桌面客户端、Codex App Server stdio 通信、线程创建与任务发送；单个任务失败后连接保持可用，运行时异常退出或启动失败会以 1/2/5 秒有限退避自动恢复，并防止已取消的启动流程回写状态或遗留进程。
- `已完成` 多工作区、窗口尺寸、主题、推理强度、审批模式和线程历史的本地持久化；审批模式在重启后恢复，默认仍为“请求批准”。左侧栏直接展示并切换工作区列表，可新建、保存、切换或移除记录。新建项目使用 Codex 风格弹窗，可选项目名称，源文件夹支持点击选择或拖拽目录；项目具有独立身份与任务归属，新项目从空列表开始，不会载入同目录既有 Codex 会话。每个工作区独立保留主目录与多个附加目录；项目独立历史升级会将原目录索引缓存自动迁移到项目 ID，写入任一项目时会保留其余加密缓存并串行化并发保存，防止切换或重启后任务列表丢失。新建、恢复或切换后自动连接，附加目录可随时增删且只影响后续新任务。
- `已完成` 已查看的任务完成提醒写入工作区加密历史快照；重启或侧栏重建不会重复显示，任务再次开始新一轮执行时会清除已查看状态。当前已打开的任务完成时直接视为已查看，不显示完成提醒；蓝色提醒仅保留给未查看的完成结果，切换或新建项目不会继承上一项目的已查看 ID。
- `已完成` 项目“编辑”入口对齐 Codex 桌面端：项目名称可编辑并持久化，名称输入框保持单层外框，主/附加源文件夹以分组卡片展示，可添加或移除附加文件夹，并支持移除当前本地项目记录而不删除磁盘目录。
- `已完成` API Key / 浏览器登录、Codex 原生 Provider 配置接入和运行时诊断脱敏；应用不再维护独立中转站配置，并通过 App Server 只读展示当前项目最终生效的模型、Provider 与字段来源。
- `已完成` 从 App Server 模型目录切换后续新任务的模型，并按模型能力联动推理强度；模型和推理强度控件不绘制额外边框，推理强度仅显示“默认”“高”“中”“低”等名称；保留“跟随 Codex 配置”，且不覆盖历史任务模型。
- `已完成` 对话历史恢复、线程搜索、置顶、归档、本地历史导入导出和 App Server 分页补齐；当前项目的普通任务行使用 4px 外边距、30px 树形文字缩进和 32px 高度，行尾运行指示器为 12px；悬停显示置顶、归档快捷图标，右键打开完整任务菜单，替代常驻三点入口。任务搜索改为项目列顶部图标触发的 Codex 风格弹层，支持跨已加载项目按标题、预览、项目或 Provider 查找并恢复任务，且提供新聊天、打开文件夹和文件变更搜索快捷操作。任务运行中可从搜索结果切换同项目任务；跨项目结果继续禁用，避免中断后台执行。若另一 Codex 客户端仍持有任务 writer，恢复或归档会显示可重试的非阻塞提示，不污染当前时间线；归档重试会包括冲突项及批量操作中其后尚未处理的任务。若任务已在其他客户端归档，恢复会要求先取消归档，任务回到活跃列表后才可重新打开。提示与操作严格绑定原项目，切换工作区即清除。
- `已完成` 切换任务首次加载时显示 Codex 风格历史加载页并缓存最近 8 个任务的完整时间线页面和滚动位置；再次切换直接显示已保活页面，任务切换与实时回复的自动定位都不播放平滑滚动或重新定位动作；文本选择不创建嵌套滚动区，滚轮始终驱动整条时间线；超出上限的较早任务重新读取历史；历史读取失败不会显示前一任务内容。
- `已完成` 随任务发送的本地图片会在用户消息气泡中显示缩略图，并随时间线缓存图片路径。
- `已完成` 左侧导航按 Codex 风格整理为项目树：工作区使用文件夹节点，任务作为各项目下的文件缩进显示；启动时预读全部项目的本地任务缓存并默认展开，只有显式点击项目行的展开/收起按钮才会在当前会话中改变显示状态，切换项目不会收起其他项目。其他项目任务为只读预览，打开时会先切换到所属项目并重连运行时，不允许在当前项目中继续、置顶、归档、重命名或删除。置顶任务独立分组，选中任务使用低对比圆角背景，每个运行中的任务在行尾显示高对比加载指示器，当前任务可点击保持焦点；选中态与 loading 仅属于当前项目，其他项目的缓存任务不会继承。刚创建但尚未进入服务端列表的任务会先显示在侧栏，即使用户立即新建或打开另一项任务，也会保留所有仍在启动的后台任务占位行，未查看的已完成任务显示蓝色完成提醒且打开后自动清除，错误任务显示红色叹号；悬停项目自动展示详情卡片，详情卡片支持持久化项目置顶并读取当前项目或本地缓存任务数，更多按钮可打开置顶、编辑、工作树、归档和移除菜单；运行中仍可新建任务或切换至另一个同项目任务，旧任务在后台继续执行并在完成时更新状态；打开运行中后台任务会通过只读历史加载，不重复申请 App Server writer，且保留 `turn/steer` 调整方向能力。旧版运行时缺少任务 ID 的完成通知会通过权威任务列表对账清除后台状态，后台审批面板会标明所属任务；为保留后台任务，运行期间不能切换项目、重连运行时或修改插件。
- `已完成` 接入 `turn/plan/updated` 实时计划通知，以悬浮步骤面板展示待执行、进行中、已完成状态和当前步骤位置；任务结束后自动收起。
- `已完成` 隐藏 App Server 命令输出增量等协议级执行事件，避免它们作为“执行事件”重复占据对话时间线；审批、任务结果和恢复历史中的实际命令记录继续展示。
- `已完成` 连续的历史命令、文件读取和工具调用以 Codex 风格活动清单归组显示；清单默认折叠、可点击展开或收起，并保留每项的完整事件详情提示。
- `已完成` 对话时间线接入 App Server 的 `item/started` / `item/completed` 命令生命周期：运行中只显示一条简洁、可复制且可截断的当前命令，完成后归入既有活动清单；无命令执行时显示更明亮、较快扫光与三点呼吸节奏的 Codex 风格低干扰“正在思考”行，系统要求减少动态效果时保持静止，任务结束后自动移除；每个结束的 turn 使用服务端耗时（缺失时安全回退）显示“已处理”时长，并随本地历史恢复。来自此前已恢复任务的后台或迟到生命周期事件不会污染当前任务的时间线、命令、计划、文件变更或耗时；切换任务后迟到的停止请求结果与无任务 ID 的旧版完成通知也只会更新原任务状态。
- `已完成` Codex 回复按 GitHub Flavored Markdown 渲染，支持正文选择复制及标题、列表、加粗、链接、引用、表格和代码块；用户输入、命令与系统记录维持纯文本，避免将日志内容误解析为 Markdown。
- `已完成` 支持在运行中任务的 Composer 中先暂存待发送方向：按 Enter 只生成非持久化临时项，不写入实际对话记录；该项显示在 Composer 自身的顶部操作栏，而不是会话气泡或滚动时间线中，文字位于左侧，“调整方向”和删除操作位于右侧。点击“调整方向”后直接经 App Server `turn/steer` 发送到当前活动 turn，不弹出二次编辑器也不新建线程，删除可丢弃尚未发送的方向，服务端返回的 turn ID 会继续用于后续调整；空 Composer 不会生成无意义的待发送项，切换任务后迟到的发送结果也不会污染新任务。运行中仍可附加文件、图片、文件夹、技能、目标和计划上下文，停止请求会携带当前 thread 与 turn ID，且仅在活动 turn 已知时开放。
- `已完成` 接入 Riverpod 管理应用共享控制器的所有权与订阅；保留 `CodexController` 的 `ChangeNotifier` 桥接以兼容既有业务状态与测试注入。
- `已完成` 将主题偏好纳入独立 Riverpod Provider，并统一工作区管理、账户、运行时、归档、文件审查、插件、marketplace 与 Git 弹窗的共享控制器订阅；测试和嵌入场景仍可显式注入控制器。
- `已完成` 插件 marketplace 管理、启用/停用配置的原子写入，以及 Git 状态、Diff 和显式提交工作流视图；插件命令复用自动发现的 CLI 绝对路径，不受桌面应用 `PATH` 限制，路径解析和命令调用均有 20 秒超时保护。
- `已完成` 输入区“添加”菜单、桌面拖拽与剪贴板附件：支持文件/文件夹与项目上下文、线程目标、计划模式、技能录制和动态技能选择；文件可拖入，也可从 Finder 复制后在输入框粘贴，剪贴板屏幕截图会作为临时 PNG 图片附件发送，并在时间线仍引用时保留、待不再引用或页面释放时清理；图片附件支持缩略图和可缩放的大图预览，附件自动去重，普通文本粘贴仍保持原有行为；并通过 `localImage`、`thread/goal/set`、`collaborationMode` 与结构化 `skill` 输入接入 App Server。
- `已完成` 右侧检查器对齐 Codex 桌面端的环境信息卡：展示本地变更、分支、提交/推送、创建拉取请求和比较分支入口；标题、操作行、分组和文件行采用 18/14/14/13px 紧凑字号；Git 视图支持文件级暂存、确认后还原、显式提交/推送及通过本机 GitHub CLI 创建 PR。
- `已完成` 左侧任务栏与右侧环境栏支持通过 Codex 风格的细分隔条拖拽调整宽度，并在窄窗口保留右侧自动收起行为。
- `已完成` 将工作区改为左右两列主结构：项目列和工作台列各自拥有独立顶部栏；环境信息在宽屏下保留为工作台内部的辅助检查区。
- `已完成` 将任务名称、Provider / App Server、沙箱边界与文件变更入口收拢到工作台顶部；当前任务名称支持点击后行内编辑，Enter 保存、Esc 取消。
- `已完成` 统一 tooltip、项目详情与文件 Diff 悬停预览的 450ms 显示延迟，快速掠过控件或列表行不会弹出临时浮层。
- `已完成` 隔离测试运行时配置，避免测试将 macOS 系统临时目录写入开发版工作区偏好；应用会拒绝该目录作为项目或附加目录，并在恢复时自动清理旧记录。
- `已完成` 收紧侧栏任务选中态的垂直内边距，并以顶部图标打开的搜索聊天面板替代常驻搜索框，保持项目树扫描密度。

## 待办

### P0：发布与可靠性

- `已完成` 建立未签名 Release 打包流程：`build_dmg.sh` 会按 `pubspec.yaml` 版本生成 DMG，发布说明见 `RELEASE_NOTES.md`；2026-08-21 已生成并通过 `hdiutil verify` 校验。
- `待办` 在干净 macOS 用户账户或测试机完成 DMG 安装回归；步骤见 `RELEASE_CHECKLIST.md`，需要人工操作安装后的应用。
- `受阻` 完成首次 Developer ID 签名和 notarization 提交。`build_dmg.sh` 已支持签名应用与 DMG、`notarytool` 提交、staple 和 Gatekeeper 评估；仍需提供 Apple Developer 证书、Keychain notary profile 与发布权限，未具备前只能生成未签名 DMG。
- `已完成` 为安装脚本增加“仅构建、不安装”和“仅安装、不构建”参数；正常退出请求被取消时会对目标应用进程回退使用 `SIGTERM`，且仅在 `/Applications` 不可写时请求管理员权限。
- `已完成` 增加 CLI 不可用的启动失败、可复制脱敏诊断和重试回归测试。

### P1：核心体验

- `已完成` 当历史任务被另一个 Codex 客户端持有 writer 时，以输入区上方的非阻塞“已在另一个应用中打开”提示说明状态，并提供关闭另一处会话后的重试操作，不再弹出对话框。
- `已完成` 将项目栏顶部的紧凑图标替换为“新对话、拉取请求、已安排、插件”文字入口，并让后三项打开 Codex 风格的独立右侧工作区；已安排任务会在桌面应用运行期间按本地持久化的时间发送到对应项目，避免抢占正在运行的任务。插件页提供“插件 / 技能”切换、真实 App Server 技能列表和右上角添加菜单；创建插件预填 `$plugin-creator`，录制技能会在新对话中启用结构化技能录制上下文，marketplace 表单支持 GitHub、Git URL 和本地目录，Git 引用仍由 CLI 默认设置解析；PR 页面保留 GitHub CLI 工作流。
- `已完成` 项目栏“新对话、拉取请求、已安排、插件”入口统一使用 14px 辅助导航字号，保留现有点击面、图标与键盘/语义操作。
- `已完成` 修复已安排任务在等待项目切换或运行时重连期间被取消后仍可能发送的问题；取消现在会使该后台调度失效，不会创建新对话。
- `已完成` 修复新建任务或切换历史任务后，旧任务迟到的生命周期和计划通知可能写入当前视图的问题；项目详情卡片的任务数每次打开都会从本地缓存刷新。
- `已完成` 修复文件变更统计将仅含 Diff 头、二进制或元数据的不可计数变更误报为 `+0 -0`；这些情况统一显示未知统计。
- `已完成` 修复任务切换与异步列表刷新交错时，已打开任务重新显示旧的完成提醒；打开任务会立即确认提醒。
- `已完成` 增加线程永久删除与批量归档确认操作；删除会提示 App Server 派生线程影响并移除本地缓存引用，归档后仍可恢复。
- `已完成` 增加 Diff 文件状态筛选、路径搜索与超大预览截断提示；Git 状态与 Diff 保持只读，文件级暂存、确认后的还原、提交、推送与 PR 创建只会在用户显式触发后执行。
- `已完成` 文件变更不再逐条显示在会话中，统一通过任务完成后的摘要卡片、悬停 Diff 预览和只读审查入口呈现；预览会在窄窗口中自动限制尺寸并选择可见方向，显示真实 hunk 行号，且会跟随异步补齐的文件 Diff 更新。
- `已完成` 收紧文件变更摘要卡片的排版：标题采用 14px 半粗体，新增/删除行统计采用 13px 字号，保持对话时间线的信息层级。
- `已完成` 收紧 Composer 默认输入文字至 14px，并将发送与运行中任务的停止按钮统一为 36px 圆形点击面；上箭头和停止图标保持居中，并保留各自语义样式。
- `已完成` Composer 的 Enter 发送会识别输入法未确认的组合文字：拼音等组合态下，Enter 继续交由输入法取消或确认候选，只有组合结束后才会提交消息；候选确认后不会继续吞掉正常发送的 Enter。
- `已完成` 在任务输入框顶缘以悬浮提示显示文件变更统计，显示文件数量及新增/删除行数且不挤占输入区；Diff 不可用时保留未知统计语义。
- `已完成` 增加插件操作的具体进度、失败原因与“重启运行时后生效”状态反馈；成功重启后会更新提示。
- `已完成` 为运行时诊断提供导出到本地文本文件的入口，并在导出前再次执行凭据脱敏。

### P2：质量与可维护性

- `已完成` 为历史缓存、插件配置与操作反馈、Git 解析/筛选/大 Diff 和脱敏规则补充分层单元测试及异常路径测试；同时覆盖运行时失败重试、注入控制器重建、线程删除事件与批量归档部分失败/重复提交。
- `已完成` 建立 macOS GitHub Actions CI：格式检查、`flutter analyze`、`flutter test` 和 macOS Debug 构建；`yeknom_ui_kit` 已改为固定提交的 Git 依赖，不再依赖仓库外本机路径。
- `进行中` 完善键盘导航、焦点顺序、语义标签和高对比度主题；本阶段新增操作均有文字标签或 tooltip，完整无障碍审计待后续集中执行。
- `已完成` 评估 Windows / Linux 平台支持；结论和启动前置条件见 `PLATFORM_SUPPORT.md`，在 macOS 发布前不启动实现。

## 下一项建议

优先在干净 macOS 用户账户或测试机执行 DMG 安装回归；取得 Apple Developer 凭据后，再完成首次签名、公证与 notarized DMG 回归。
