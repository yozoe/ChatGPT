# Codex Desk Agent 约定

## 文档同步

涉及用户可见行为、交互、数据边界或维护流程的代码改动，完成实现后必须同步更新文档：

- `README.md`：更新“当前进度”中对应能力的实际行为和限制。
- `ROADMAP.md`：更新唯一任务清单中的状态；已交付能力归入对应的“已完成”小节。
- `RELEASE_NOTES.md`：面向未发布版本记录用户能感知的新增、修复或行为变化。

文档描述当前实现，不记录临时调试过程或未实现的设想。若改动只影响测试、内部重构或不会改变用户可见行为，可不新增发布说明，但应在代码审查中说明理由。

## 验证

提交前运行 `dart format`、`flutter analyze` 和相关 `flutter test`；涉及跨窗口布局或悬停交互时，应补充覆盖窗口边界、生命周期和异步状态更新的测试。

## 提交规范

所有 Git 提交信息必须使用英文；提交标题和正文均不得使用中文。

## 状态管理

涉及跨组件共享、异步加载/错误/刷新状态、缓存，或需要协调多个界面的复杂状态时，必须优先使用 Riverpod 管理；仅限单个组件且生命周期明确的临时交互状态，才保留在局部 `StatefulWidget`。

## Dart 类型边界

每个 Dart 文件最多定义一个 `class`；新增或迁移的类必须使用公开命名，并按职责放入独立文件。所有 Dart 文件都不得定义私有类（名称以 `_` 开头），也不得通过 `part` / `part of` 将类隐藏在同一个 library 中。修改既有代码时，若触及多类文件或私有类，应一并拆分为一类一文件并迁移为符合该规则的公开类型。

## Codex 一致性

所有新增或修改的功能与界面都应无限接近 Codex 桌面客户端：优先复用其信息层级、交互语义、键盘与悬停行为、深浅色表面和控件节奏；若本地业务边界必须不同，应保留 Codex 的视觉语言并在文档中说明差异。
