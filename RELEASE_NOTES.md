# Codex Desk Release Notes

## Unreleased

- 新增设置页“已归档的聊天”：接入 Codex App Server 当前项目归档线程列表，支持搜索、当前项目筛选、按项目分组、取消归档、单条永久删除及全部删除确认，并显示加载、空列表和错误状态；运行时未就绪或存在运行中任务时删除操作会禁用。
- 修复应用启动自动连接运行时时左侧任务列表点击无反应的问题：恢复的任务现在可在运行时就绪前立即切换本地缓存视图，连接完成后自动恢复远端任务。
- 修复会话时间线在滚动结束与列表布局同帧发生时可能抛出 “Build scheduled during frame” 并中断渲染的问题；“滚动到最新消息”提示会在该帧结束后安全更新，正常手势反馈不变。
- 左侧栏顶部品牌已更新为 17px 粗体 “Xedoc”，并移除前方绿色星标图标。
- 修复结绳 Dock 图标比命令云视觉偏大的问题：现为结绳图标加入与命令云相同的透明安全边距，切换图标后尺寸保持一致。
- 会话未滚到底部时，Composer 上方新增居中的下箭头提示；点击会以约 260ms 平滑滚回最新消息并恢复实时内容跟随。
- 修复“编码 > 钩子”设置页：现在异步显示 Codex 实际发现的项目、用户和插件钩子，以及加载、空列表和读取错误；运行时未启动不再显示为“无钩子”。信任状态按当前钩子定义哈希核验，定义修改后会重新要求审核；启用、停用、信任和撤销信任会调用 Codex 的持久化配置接口，而不直接编辑 `config.toml`。
- 修复助手最终回复显示在创建和命令活动之前，以及位置重排导致流式文字被拆成多条的问题。客户端现在按 App Server `agentMessage.phase` 区分过程说明和最终答案；真正发出 `turn/start` 前收到的迟到增量或完成通知不会污染新任务。单条消息继续连续流式更新；未提供 phase 的旧版运行时通常保持原始顺序，已缓存成“耗时后仍有过程输出”的损坏记录会按生命周期边界恢复。
- 调整完成任务的“耗时”展开布局：耗时现在作为该轮任务明细的标题显示在顶部，展开后再显示任务创建、过程说明、命令活动和最终答案，完成状态位于末尾，不再把耗时开关放在执行明细底部；标题箭头和明细区域以 180ms 动画同步过渡，文字会随顶部对齐的展开区域淡入，而非突然显示。
- 新增设置“配置”页：可修改会保存的批准策略与后续新任务推理强度；沙盒、网络、输出和推理摘要等由 Codex App Server 管理的项目会清晰说明边界，并提供模型、Provider 与字段来源状态查看和 CLI 诊断入口。
- 修复设置“配置”页在窄窗口可能横向溢出的问题；沙盒、网络、输出和推理摘要的只读状态不再伪装为可打开的配置入口。
- 设置导航顶部的“返回应用”和“搜索设置”现固定可见，滚动时仅移动分类菜单。
- 设置导航中尚未实现的入口现显示“（待开发）”后缀。
- Dock 图标选择会保存到 macOS 偏好；应用重启后继续使用上次选择，不再固定回退为命令云。
- 外观设置中的 Dock 图标新增橙色结绳选项；两个图标以相同视觉尺寸展示，点击后无需重启即可立即更新 macOS Dock 图标。
- 结绳 Dock 图标改为与命令云一致的深棕渐变底和柔和橙色叶片；外观设置左侧直接预览该 SVG，切换任务恢复历史使用从该图标单独导出的透明叶片图片，不带底板。
- 新增 macOS 本地任务完成通知：任务成功完成时会显示系统通知，并在 Dock 图标显示红点；查看所有本次运行中新完成的任务后红点自动清除。通知权限被拒绝不会影响任务执行。

- 文件变更摘要中的 SVG 文件现在会在鼠标悬停时直接显示图片预览。
- 新增外观偏好页：可选择参考图结绳 Dock 图标或既有命令云图标，并提供指针光标、动态效果、字号、差异标记和字体平滑控件。
- 新增左侧菜单底部的设置与帮助入口：点击设置后整个左栏替换为与主侧栏保持相同宽度的设置导航，右侧提供常规权限、工作区目录、默认编辑器、终端位置、主题、运行时、插件、账户和快捷键等入口；问号菜单提供 Chrome 扩展、快捷键对话框和帮助中心链接，并支持窄窗口布局。
- 修复从拉取请求、已安排或插件页面点击左侧任务后无法返回会话的问题；任务搜索结果现在也会正确切回会话并打开任务。
- 优化侧栏项目节点的展开辨识度：展开和收起分别显示 Codex 风格、轮廓明显不同的打开与关闭文件夹图标。
- 优化插件默认图标：缺少或无法读取插件 logo 时显示 Codex 风格的旋涡标记，不再显示拼图图标；左侧“插件”入口复用该标记，所有左侧菜单项的图标与文字固定在 32px 行高内垂直居中。
- 修复插件页已安装插件重复出现在精选/分类目录的问题，并限制 marketplace Logo 只能读取插件源目录内的文件。
- 修复会话中的项目文件行将长文件名显示为省略号或被固定宽度提前折行的问题：文件名现在会先占用会话的全部可用行宽，仅在超过容器时自然换行并完整显示。
- 审批请求现在采用贴近 Composer 的 Codex 悬浮卡片，显示终端图标、请求原因和等宽命令块；命令和额外权限支持“允许一次”、拒绝（Esc）及下拉菜单中的“允许类似操作”，文件变更仅发送单轮允许，分别遵循 App Server 的决策范围。
- Composer 模型选择器左侧新增背景信息窗口指示器；点击后显示当前对话上下文的本地估算使用量、百分比、已用/上限标记数，并在接近上限时提示 Codex 可能压缩较早内容或建议开启新任务，服务端尚未提供精确 token usage 时会明确标注“估算”。
- 用户消息气泡继续保持右侧布局，内部单行与换行文本维持自然左对齐。悬停已发送的用户消息时，时间旁提供复制和修改图标；复制会写入系统剪贴板，修改会原位展开 Codex 风格的内联编辑器及取消、发送按钮。发送修订内容会作为下一轮任务提交，运行中则安全排入调整方向，不会改写既有会话记录。
- 收紧“已运行命令”等活动摘要及展开明细的上下间距，让时间线活动清单更接近 Codex 的紧凑节奏。
- 优化项目树展开/收起动画：任务区域沿高度平滑过渡，快速反向点击也能自然衔接。
- Composer 的 Codex 风格 `/` 菜单现在会在输入空 `/` 时先显示全部快捷指令，并在其后显示当前项目已启用的技能列表（名称、简介、个人/项目/系统范围）；继续输入文字会持续筛选快捷指令与技能，而不会关闭菜单。鼠标、↑↓、Enter 或 Tab 选择时会自动将高亮项滚入可视区，并以结构化 skill 上下文加入输入区；已选技能会显示为紧凑的蓝色 Codex 徽标，点击可查看说明、范围、来源路径及本地 `SKILL.md` 内容，读取失败也会直接显示原因，关闭按钮仍可移除该上下文；选择“目标”会切换为带底部标签的内联目标输入；选择“MCP”会保留 `/` 并显示真实服务器认证/启用状态，未知认证状态不会误标为不支持；MCP 面板中的 Enter 会关闭面板而不再把 `/` 发到会话，刷新失败时仍显示的缓存服务器会标注为上次读取结果；选择“代码审查”会保留 `/` 并显示紧凑的范围选择框，Enter 可直接选择“审查未提交的更改”并发起当前 Git 工作区审查；运行中任务尚未接收方向时会显示等待状态，随后自动排队，不再闪退或误把 `/` 发送到会话。
- 优化会话中的活动摘要：标签和图标改为更低对比度的灰色；收起时显示右向箭头、展开时显示下向箭头，箭头旋转和固定整行宽度的详情区域仅沿高度平滑展开/收起。
- 优化侧栏任务列表密度：项目节点与普通任务行收紧至约 32px 高，任务标题在固定行高内容区内垂直居中，移除行间多余空白，让连续任务的扫描节奏更接近 Codex 桌面端；点击整个项目行可展开或收起任务列表，左侧文件夹图标以打开/关闭状态表达层级，并在项目与任务背景之间保留细微留白；批量选择行固定为 56px 以容纳复选框点击区域，悬停操作与任务缩进保持不变。
- 优化会话自动贴底调度：流式内容更新会合并有限的布局校准，避免重复滚动回调造成滚动抖动或拖拽争抢。
- 修复会话仍停留在末尾时后续内容不再自动显示的问题：自动滚底和正常布局更新不会误设为用户上滑，只有实际离开末尾阅读才会暂停跟随。
- 修复会话时间线慢速上滑时滚动条拇指逐渐变高、滚动位置像被弹回的问题；常见会话的可变高度历史项会提前布局，滚动经过 Markdown、活动清单或文件摘要时不再反复改写滚动范围。
- 收紧 MCP 表单 schema 校验：枚举默认值必须属于枚举且枚举项不能重复；不符合条件的请求会安全拒绝，不会导致表单构建异常。

- 修复会话时间线刚从底部向上阅读时的上下颤动：完成提醒、Composer 尺寸变化和延迟贴底不再把滚动位置拉回末尾；窗口扩大而自然显示末尾时，完成提醒仍会正常清除。
- 修复 Composer 上方“文件已更改”统计浮层拦截滚轮和触控板滚动的问题；该提示现在只展示信息，滚动会直接交给下方时间线。
- 支持 MCP 服务器发起的结构化输入与 URL 确认请求：表单和链接均需用户显式确认，链接不会自动打开，“帮我批准”不会自动代为提交；请求来自后台任务时会显示所属任务，格式损坏或不支持的表单会安全拒绝而不再显示“未支持的运行时请求”。
- 支持任务执行期间直接切换到其他项目的任务并继续操作；运行中的 turn 复用共享 App Server 按工作目录隔离，在原项目后台持续执行，完成状态和未查看蓝点只写回所属项目，不再要求等待任务结束或中断运行时。
- 修复运行期间快速连续切换项目可能被较早的历史恢复和配置保存覆盖、同一后台项目多个任务同时完成时缓存互相覆盖，以及旧版无任务 ID 完成通知只能对账当前项目的问题。
- 修复项目切换期间排队的本地历史保存可能串到另一个项目的问题；每个快照现在固定写入其所属项目，避免 Sourcetree 的消息出现在 ChatGPT 项目下。
- 修复从项目节点悬停操作中新建任务时仍使用当前项目的问题；现在会先切换到操作所在项目并连接运行时，再发送新消息。
- 修复显式切换项目后恢复已完成任务仍显示蓝点的问题；这次切换会被视为已查看，应用启动时的自动恢复提醒规则保持不变。
- 修复跨项目快速切换时迟到的任务确认写入错误项目的问题；确认操作现在会校验任务所属工作区。
- 修复已查看的完成任务在所属项目变为非当前项目后重新显示蓝点的问题；非当前项目侧栏现在使用该项目自身缓存的已查看状态。
- 修复大量历史会话下侧栏滚动偶发卡顿的问题：项目树改为按视口惰性构建，流式状态和完成提醒更新不再重新布局全部会话行。
- 创建项目弹窗选定首个源文件夹后可继续添加多个目录；首个目录作为主目录，其余目录会随项目保存为附加目录，并可在创建前逐项移除。
- 修复会话流式输出的当前回复仍会快速上下抖动的问题：未完成回复不再随每批 token 重解析不完整 Markdown，而是使用固定行高、只追加的稳定文本布局，既有回复继续复用缓存渲染。流式内联链接在目标闭合前不显示临时标签，闭合后本地链接直接采用最终文件名，隐藏的长路径不会参与换行；代码围栏、行内代码和转义结构保持原文。第一段文字前仍显示“正在撰写回复”，正文出现后由回复自身承接状态，不再额外保留会在 item 完成时带动正文位移的状态行。包含本地文件链接的回复在消息完成后也会保持该布局，待所有链接去重批量校验完成，再一次性显示最终 Markdown 和全部文件行；文件行只向下展开并保持回复顶部锚点，不会为了重新贴底把已显示文字向上推。单个链接解析异常会降级为普通链接，不再阻塞整条回复。
- 修复审查界面首版中的可靠性与窄屏问题：Git 写操作失败现在会显示具体原因，提交成功但推送失败会明确区分；大量工作区文件的 Diff 读取限制为最多 6 路并发；同一文件的暂存/未暂存复合 patch 会分别重置行号；宽屏打开后可直接按 `Esc` 关闭；长文件树会把当前文件滚入可见区；极窄窗口把提交和 PR 收进更多菜单，不再产生工具栏溢出。
- 将任务文件“审核”从模态弹窗升级为 Codex 风格工作台审查界面：宽窗口与会话并排并可拖动调宽，窄窗口覆盖会话但保留 Composer 草稿、时间线和审查状态。新界面可在“最新一轮”与“Git 工作区”间切换，连续显示多文件 Diff、旧/新行号、Hunk 和未修改区域，并提供可筛选/折叠/跳转的目录树、共享长行横向滚动、正文滚动联动，以及全局互斥的暂存、确认还原、提交/推送和创建 PR 操作。
- 修复任务完成提示出现/收起及用户消息时间戳悬停时改变时间线高度、导致文字快速上下跳动的问题；两类短暂提示现均作为不参与布局的悬浮层显示。
- 修复同项目任意任务运行时归档按钮看似可用、实际却被静默忽略的问题：归档现在只检查目标任务自身状态，其他前台或后台任务不会阻塞空闲任务；运行中的目标任务会提示先停止，确认期间发生状态变化也会按运行中、处理中或运行时不可用给出明确反馈。项目菜单“归档聊天”现会真正进入批量选择，确认后跳过运行项并反馈数量，取消会保留选择且不显示误导提示；等待 App Server 确认期间任务行显示处理中进度，当前线程 Composer 同步禁止发送新任务。
- 修复短计划使用最大约束高度定位三点 loading、导致图标远离计划面板甚至在小窗口越界的问题；loading 现在与实际计划内容组成同一个底部自适应浮层并保持稳定间距。
- 修复 Markdown 强制换行后的项目文件链接与上一行文字垂直错位的问题；文件行现在会正确换到下一行并与正文左侧对齐。
- 移除任务运行期间 Composer 上方重复的“正在处理任务”状态胶囊；三点 loading 改为固定悬浮在 Composer 上方，不再作为时间线末项跟随消息滚动，具体实时活动继续保留在时间线中，完成与连接异常提醒保持不变。
- 增加会话滚动区域的底部安全间距，避免滚到底时三点 loading、分割线或文件变更统计紧贴悬浮输入区。
- 对话时间线现在延伸到悬浮输入框后方，滚动文字接近输入区时会经过透明渐变自然隐去；输入框作为上层实体轻微覆盖渐变底端，输入区高度变化会同步更新末尾留白，最后一条内容仍可完整滚出。无明确实时活动时的“正在思考”文字改为居中的圆形三点 loading，三个点以错开的相位呈波浪状上下跳动，并在系统要求减少动态效果时保持静止。
- 修复应用启动后首次打开历史会话总是停在开头的问题：加载过程中刚生成的内存缓存不再被误判为已保活页面；长会话会在惰性列表范围稳定并定位到最后一条记录后再显示。加载与校准状态按会话隔离，即使在校准期间快速切回另一个已打开会话，也会取消旧校准并保留原有阅读位置。
- 子智能体状态徽标现可打开只读子线程检查器：宽窗口在右侧展示，窄窗口以覆盖层显示可返回详情页，打开、关闭或改变窗口宽度时会保留未发送文字、附件、Skill、目标和计划模式，并支持继续查看嵌套子智能体。完整子线程历史按最近使用顺序最多缓存 8 项，切换工作区会取消旧刷新并拒绝迟到结果；运行时停止、重启或意外退出时，在途读取会立即结束 loading、保留已有内容并显示可重试提示，旧连接结果不会回写，运行中的旧状态也不会继续伪装为工作中。
- 修复流式输出及迟到条目插入前方时整段会话文字反复重排和抖动的问题：时间线条目现使用可持久化稳定 ID，惰性列表会在索引变化后迁移原 State；已完成消息继续复用现有 Markdown 渲染子树，活动与“耗时”折叠状态保持不变，新 token 只更新发生变化的回复。
- 修复插件、marketplace、Git 摘要/Diff 与 Codex 配置的较早异步刷新可能覆盖较新结果的问题；项目悬停卡任务数改由 Riverpod 按项目加载并随卡片自动释放，嵌入场景替换控制器后操作也不再误发给旧实例。
- 新建项目与切换项目不再共用运行时锁：前台或后台任务运行、运行时启动及插件配置保存期间仍可创建项目；新项目只保存为非活动项目并建立空任务边界，不切换当前工作区、不停止任务或重连 App Server，也不改写当前项目的旧版附加目录偏好，避免部分保存失败后重启又出现已回滚项目。切换项目仍需等待相关操作完成，并会显示具体原因。
- 移除全局 Material 点击水波纹：按钮、任务行、菜单项和其他点击面不再显示扩散动画，原有悬停、选中及键盘焦点反馈保持不变。
- 修复当前任务完成后“任务已就绪”和侧栏蓝点持续不消失的问题：时间线已经位于底部时会立即自动确认；用户停留在上方阅读时继续保留提醒，随后滚到底部，或扩大窗口使时间线末尾自然可见时再自动隐藏，且不会误确认后台任务。
- 调整截图风格的视觉层级：正文收紧到 12–13px，侧栏和辅助信息使用更低对比度灰色，Markdown 标题与正文不再过大过亮；侧栏、顶部工具栏、环境信息和活动状态图标同步缩小，关键发送/停止操作保持原有辨识度。八套兼容预设现在拥有可区分的强调色，浅色主题按钮会自动选择满足对比度的前景色。
- 移除已完成任务“耗时”展开按钮的外圈描边，文字、箭头、圆角点击反馈与下方分割线保持不变。
- 会话时间线现在只在“已处理 X 秒”和完成后可展开的“耗时”下方保留分割线；其他消息、活动和系统记录之间仅保留原有空白间距。
- 修复从历史任务进入“新对话”后，上一任务的首条用户消息仍会出现在空白时间线的问题；新建任务现在只保留真正的内置欢迎项。
- 修复运行中“已处理”计时器始终跟随最新活动停在会话底部的问题；计时器现在固定在本轮用户消息之后，流式回复、文件操作与命令状态会像 Codex 桌面端一样继续显示在其下方。
- 修复刚发送的剪贴板截图可能立即提示“图片临时文件已经失效”的问题；临时图片现在由会话控制器统一持有并记录 Composer 保留计数，在当前/缓存时间线、排队方向、运行中提交或失败重试仍引用时，不会再因切换功能页、Composer 重建或控制器切换被提前删除；最后引用消失时即使 Composer 未挂载也会主动清理，避免长时间运行后累积临时文件。
- 修复会话文件行的安全与渲染回归：文件在条目显示后被替换为项目外符号链接时，点击、Markdown 预览读取和外部打开均会重新校验并拒绝越界；普通链接不再丢失粗体、行内代码或链接图片等嵌套 Markdown 样式，链接内本地图片也会限制在当前项目并向读屏软件提供替代文本；助手回复在隐藏可见身份标签时保留读屏语义，延迟完成布局的文件行即使位于 Markdown 表格内，也会在用户仍停留底部时继续贴底。
- 连续命令之间的“已自动批准本次操作”现在会与命令一起收纳进同一个活动摘要，不再重复显示多个“已运行了命令”；展开后仍按原顺序保留每条命令和自动审批记录，手动审批与错误继续独立显示。
- 会话时间线中的助手回复现在直接显示正文，不再在每条回复左上方重复显示“Codex”标签；指向当前项目现有文件的链接会显示为紧凑文件行，缺失、越界或项目外目标仍按普通链接展示并保留安全边界；审批、错误与工具等系统记录标题不受影响。
- 修复流式推理摘要的每个文本增量都会立即触发工作台重建的问题；连续摘要现在按与回复正文一致的节奏合并界面通知，最终内容保持完整。
- 修复扩展配置无法识别 TOML 单引号表头与技能路径的问题；单引号形式的 MCP 名称和项目信任路径现在可正确判定配置范围并写回原文件，技能启停会更新原配置块而不再追加重复块。
- 技能读取与独立子代理状态现在会像 Codex 客户端一样直接显示在会话时间线中：技能完成后保留“已读取…技能”，子代理以紧凑徽标展示并从“已开始工作”原位更新为“已完成”、失败或停止。状态按 App Server 的 item / 子线程标识去重，可随本地加密历史和服务端线程历史恢复，不再在完成后消失或只收进通用工具清单。
- 子代理徽标现在可打开 Codex 风格子线程检查器：宽窗口使用右侧分栏，窄窗口切换为覆盖当前会话的详情页；检查器通过只读 `thread/turns/list` / `thread/items/list` 分页展示子代理任务说明、处理状态、分析、命令与 Markdown 结果，不会调用 `thread/resume` 抢占仍在运行的子线程 writer。子线程结束或产生新项目时会刷新内容，加载失败可在原位重试，关闭后返回主任务。
- 断网后的自动等待现与 Codex 客户端一致：App Server 发出可重试网络错误时，会在对应 turn 的对话时间线内逐次显示灰色 Wi-Fi 活动行 `Reconnecting... waiting for network`；后台任务的记录按 thread 保留，重新打开时归入自己的时间线，不会污染当前会话。任务保持运行，恢复网络后自动继续；已有后续进展或结束的记录不再作为实时读屏状态。只有 App Server 最终结束为失败时，输入区上方才显示 Codex 风格的行内“重试”；点击后会在同一线程原样重发文字、Skill/图片输入、目标、计划模式与模型设置，不重复添加用户消息。重试期间会防止重复点击，再次失败后仍可继续重试；主动停止/取消及没有原始提交上下文的历史失败不会显示该入口，迟到通知和切换任务后的异步结果也不会写入错误任务。
- 文件变更摘要的“审核”左侧新增 Codex 风格“撤销”：摘要与 Diff 现在严格限定为最新一轮，新一轮启动失败时恢复上一轮状态；仅当当前项目没有前台或后台任务运行时，应用才会由 Git 核对补丁路径与摘要文件、暂存区和当前工作树，再原样反向应用完整 Diff。执行中会锁住任务发送和重复点击，文件已有暂存或后续编辑、路径不一致、Diff 缺失或截断时不会覆盖现场，并会保留摘要说明失败原因。
- 用户消息气泡现在会在鼠标悬停时于下方显示该条消息的本地时间，移出后自动收起。
- 修复当前打开的任务完成时不显示蓝点，以及部分后台完成通知未重置已查看状态的问题；每次新的成功完成都会显示蓝点，直到用户点击对应任务行，停止或取消的任务不会误显示为已完成。
- 修复 `turn/steer` 等待响应期间切走再切回同一任务后，旧响应可能按切换前的位置重复或错序插入方向的问题；重载后的同线程历史现在会去重并保持权威顺序。
- 修复 MCP 服务器已成功写入、但后续扩展列表刷新失败时，添加窗口仍误报“添加失败”并诱导重复提交的问题；写入失败与刷新警告现在分开反馈，即使 MCP 标签自身保留刷新错误，成功操作后的警告仍优先显示并关闭表单。
- 修复未选项目时托管 MCP 被误标为用户级，以及未受信任项目的同名 `.codex/config.toml` 可能抢占有效用户配置来源的问题；项目级 MCP 现在只在 Codex 用户配置明确标记该项目为受信任时才可编辑，其余无法确定来源的服务器保持托管只读。
- 修复 MCP 管理忽略项目级 `.codex/config.toml`、可能把项目服务器启停状态写入用户全局配置，以及旧项目刷新结果可能在切换后覆盖当前列表的问题；列表现在从当前项目目录读取并标注配置范围，只写回服务器的实际定义文件，切换项目会使在途旧请求失效，无法确定来源的托管服务器保持只读。
- 修复 MCP 与技能操作失败在对应标签页没有反馈、添加 MCP 失败时弹窗无提示，以及插件设置缺少卸载入口的问题；扩展操作错误现在统一显示，添加期间会防止重复提交，插件可在确认后卸载。
- 实时活动状态现已补齐 Codex 风格的文件读取、项目内搜索、目录列出和文件编辑提示，搜索会分别展示查询词与目标目录；仅单一明确的文件动作使用专用状态，混合读取与构建、测试的复合命令会显示完整命令。推理过程会流式展示 App Server 提供的 `Planning…`、`Clarifying…`、`Inspecting…` 等具体摘要，迟到摘要不会覆盖随后开始的活动。
- 已结束任务“耗时”详情中的“已运行了命令”等活动摘要现在默认收起，只有点击摘要后才展开明细，再次点击可收起。
- 侧栏中未查看的已完成任务改为以小蓝点提醒；鼠标移入任务行时蓝点会隐藏，避免与置顶、归档快捷操作并列干扰。
- 修复已结束会话中上滑阅读后，普通状态刷新仍会把时间线自动拉回最底部的问题；已排队的下一帧滚底回调现在也会重新检查阅读位置，只有仍停留在底部附近时才继续跟随更新。
- 修复带图片的“调整方向”与任务完成同时发生时，方向虽被接受但图片从会话中消失或消息排在“任务完成”之后的问题；方向现在保留实际发送顺序，等待接受期间也不会提前清理临时图片。
- 插件页右上角设置按钮现在会打开接近 Codex 桌面端的“插件 / MCP / 技能”管理弹层：标签展示真实数量并支持分栏搜索，已安装插件、Codex CLI MCP 服务器和当前项目技能可直接启停；MCP 栏支持添加 HTTP 服务器与查看连接详情，配置变更后会安全重连运行时。
- 修复点击归档后任务仍可能留在侧栏的问题：归档成功现在会使旧的活跃列表刷新失效，归档通知也不会再从尚未更新的本地 session 元数据补回任务。
- 修复从插件、已安排或拉取请求工作区点击项目行“新建任务”或搜索面板“新聊天”后仍停留在原页面的问题；所有用户触发的新建任务入口现在都会统一返回会话工作台。
- Codex 回复中的项目内 Markdown 文件现在会在应用内打开格式化阅读器，支持预览/源码切换、项目内相对文档导航、本地图片、返回、外部打开和 `Esc` 关闭；带空格、`:行号` 或 `:行号:列号` 的目标会被正确解析并在源码模式定位。阅读器保持项目目录边界，限制单份文档为 8 MB；源码模式会截断显示超过 4096 个字符的单行并提示原始长度，避免超宽布局，且不会自动加载远程图片。
- Composer 图片附件和发送后保留在会话中的图片现在都可点击进入覆盖工作台的 Codex 风格沉浸式预览；预览会按图片原始像素和当前画布显示真实缩放比例，按钮缩放保留当前查看中心，并提供手势/键盘缩放、双击放大与复位、`Esc` 关闭，以及默认图片应用打开和另存副本。
- 模型选择气泡移除模型图标；跟随配置简化为“默认”，其他选项只显示展示名称并隐藏内部模型 ID。
- 运行中的任务现在可以连续暂存多条“调整方向”，并在 Composer 顶部按顺序堆叠；每条均可独立发送或删除。未手动发送的方向会在当前及后续任务结束后按队列顺序自动作为同一线程的下一轮任务发送。
- 方向队列现在限制为窗口自适应高度，较长时在栏内滚动；发送期间会禁用其他方向的发送操作。修复自动续发失败时既恢复待发送项又残留一条伪用户消息的问题。
- 修复手动点击“调整方向”恰好遇到任务结束时可能重复发送或卡住的问题；steering 未成功时会自动转为同一线程的下一轮任务。
- 修复重启或恢复任务后已完成命令可能消失的问题：服务端历史遗漏命令项时，应用会保留本地缓存的对应命令。
- 收紧侧栏项目详情卡片的字号，项目名、任务数/路径和“编辑项目”入口改为 16/14/14px，悬停查看项目详情时更紧凑。
- 当 App Server 已明确报告当前 `item/started` 活动时，对话末尾不再笼统显示“正在思考”：现在补全计划整理、协作任务、上下文压缩，以及网页搜索、打开和页内查找等实际状态；未知的新操作也会显示中性活动提示。协作调用与上下文压缩记录可随线程历史恢复，仅在服务端没有活动事件时保留“正在思考”兜底。
- 动态技能读取现在会显示实际技能名，例如“正在读取 Code Review 技能”，而不再泛称为动态工具调用。
- 运行中命令及所有已识别的实时活动状态现在与“正在思考”共享低干扰扫光动画；系统减少动态效果时保持静止。
- 任务运行中会按秒显示“已处理”时长；完成后改为“耗时”记录，存在执行明细时可展开或收起并查看该轮紧凑操作详情，最终回复、审批和错误不会随执行过程一起隐藏。
- 修复运行中的“已处理”计时器未启动以及应用暂停后可能少算时间的问题；现在会从当前处理时长开始，每隔 1 秒更新，并按真实起始时间纠偏。
- 修复纯文本回复的“耗时”显示无内容展开箭头、审批与错误记录被移到任务完成之后，以及辅助技术每秒播报计时的问题。

- 修复 Codex 回复中的项目内 Markdown 文件链接点击后没有反应的问题：相对路径和项目内 `file:` 链接现会交由系统默认应用打开；缺失文件、符号链接越界及项目外路径会被安全拦截并提示失败。
- 移除侧栏项目行悬停时重复出现的完整路径 tooltip；完整路径仍可在项目详情卡片中查看。
- 修复 macOS Dock 启动图标在圆角深色底板外多出无圆角黑色正方形、视觉尺寸偏大，以及通过 `flutter run -d macos` 启动时回退为 Flutter 默认图标的问题；Release 与 Debug 现均保留圆角深色底板和橙色命令云，圆角外侧为真实透明像素，并以约 10% 光学安全边距匹配相邻图标，且不再自绘 Dock 瓦片。
- 修复 Composer 在拼音等输入法确认候选时可能误发送消息的问题：即使 macOS 先清除组合范围再派发该次 Enter，Composer 也会将其交还输入法；候选确认后的后续 Enter 仍可正常发送。
- 修复已归档任务取消归档失败后恢复入口消失的问题；失败时会保留入口，供用户直接重试。
- 修复连续新建任务时，较早仍在启动、尚未进入 App Server 列表的后台任务可能从侧栏消失的问题；现在会持续保留其任务行和运行状态。
- 修复快速切换任务后的事件串扰：旧任务迟到的停止请求结果，以及无任务 ID 的旧版完成通知，不再写入新任务时间线。
- 修复另一 Codex 客户端仍持有任务 writer 时的恢复与归档处理：现会在输入区上方显示“已在另一个应用中打开”的非阻塞提示，并重试原操作；批量归档会继续处理冲突项及其后的未归档任务。不会再以对话框中断工作区、把失败写入当前任务历史，且切换项目后会自动清除旧提示。
- 修复运行时暂时断开或切换期间点击 writer 冲突提示的“重试”会丢失恢复入口的问题；现在会保留提示，连接恢复后可继续执行原操作。
- 与 Codex 的归档生命周期保持一致：其他客户端已归档的任务不再直接恢复或显示 App Server 原始错误；界面会提示先取消归档，任务回到活跃列表后由用户重新打开。
- 侧栏任务行现使用 4px 外边距、30px 树形文字缩进和稳定的 32px 高度，行尾 loading 缩至 10px、描边同步收细；悬停时直接显示置顶与归档图标，移除常驻三点入口；右键任务行可打开置顶、重命名、归档和永久删除菜单。
- 收紧项目栏“新对话、拉取请求、已安排、插件”入口文字至 14px，使其保持紧凑的辅助导航层级。
- 修复侧栏任务标题的渐隐遮罩跟随短文本结束位置的问题：遮罩现固定贴齐任务气泡末端；长标题继续以 Codex 风格右侧透明渐隐裁切，不显示省略号。
- 用户消息气泡保持右侧定位，短消息会收窄；长消息改为左对齐自然换行，避免续行贴右。
- 修复旧版 App Server 未附任务 ID 的完成事件在有后台任务时可能使当前任务永久显示为运行中的问题；权威列表报告当前任务终态后会立即解除运行状态。
- 修复用户消息气泡内的文字左对齐回归：文字现与气泡一样靠右对齐。
- 修复侧栏任务会因运行、停止、完成或服务端更新时间刷新而改变位置的问题：任务首次显示后保留原有顺序，新发现任务才会追加到列表末尾。
- 运行中任务的停止按钮现与发送按钮对齐为 36px 圆形点击面，保持原有停止语义与提示。
- 对齐 Codex 桌面端的方向调整流程：运行中从 Composer 提交的内容先作为非持久化待发送消息，显示在 Composer 顶部操作栏而不是会话气泡或滚动时间线中；文字位于左侧，“调整方向”和删除操作位于右侧。点击“调整方向”会直接发送，不再弹出二次编辑器，删除可丢弃尚未发送的方向。空 Composer 不会创建待发送项，切换任务后迟到的发送结果也不会写入新任务。
- 修复“停止当前任务”仅提交线程 ID、被 App Server 以缺少 `turnId` 拒绝的问题；停止操作现在会携带当前线程与 turn ID，并在活动 turn 尚未确定时保持不可用。
- 修复运行中任务切换后重复调用 `thread/resume` 而触发 “already has an active writer” 的问题；后台任务现通过只读历史打开，并保留“调整方向”能力。
- 修复只读历史暂时返回旧 turn ID 时，当前任务的完成通知会误当作后台事件、界面永久保持运行中的问题。
- 修复直接使用 `flutter run -d macos` 启动时 Dock 可能显示 Flutter 默认图标的问题；Debug 版现在也会显示应用的自定义图标。
- 调整自定义 macOS Dock 图标的透明安全边距，使其视觉尺寸与相邻系统图标一致，不再显得偏大。
- 修复任务运行时侧栏中的其他同项目任务无法点击的问题：现在可直接切换查看，原任务会继续在后台执行并保持状态反馈。
- 修复任务运行时“搜索聊天”会禁用所有结果的问题：现在可从搜索结果切换同项目任务，跨项目结果仍保持禁用。
- 修复已查看的完成提醒状态会写入新建项目的问题；各项目的完成提醒现在严格隔离。
- 修复旧版运行时未附任务 ID 的完成通知可能让后台任务永久显示为运行中的问题；应用会通过权威任务列表对账释放完成的后台任务。
- 修复导入本地历史未替换已查看完成提醒状态的问题；导入后的蓝色完成提醒会与文件内容一致。
- 已完成的命令、文件读取和工具调用现在默认折叠为活动摘要；点击摘要后可查看或再次收起各项详情。
- “新对话”不再因已有任务运行而禁用：可以立即开始另一项任务，原任务会在后台继续执行；所有运行中任务都会显示执行指示器，后台任务完成后会更新侧栏状态。后台任务的审批会明确标明来源，且不会写入当前对话记录。项目切换、运行时重连和插件修改仍会等待全部任务结束，避免中断后台执行。
- 任务仍在处理但当前没有运行命令时，对话末尾现在会显示低干扰的“正在思考”状态；执行命令时自动切换为现有命令状态，任务结束后自动消失。
- “正在思考”状态现在使用 Codex 风格的柔和文字扫光与三点呼吸动画；系统要求减少动态效果时会保持静止。
- 加快“正在思考”的文字扫光节奏；当前已打开的任务完成时会自动确认，不再显示多余的蓝色完成提醒。
- 进一步加快并提亮“正在思考”的扫光，使运行中的状态更易辨识。
- 已查看的“任务已完成”提醒现在随工作区加密历史持久化；重启应用或重建侧栏后不会重复出现，同一任务再次执行时会重新提醒。
- 收紧 Composer 默认输入文字至 14px，并将圆形发送按钮从 40px 缩小至 36px，保持上箭头居中和既有发送语义。
- 项目栏的紧凑“新建、刷新、本地历史”图标已替换为 Codex 风格的“新对话、拉取请求、已安排、插件”入口。拉取请求、已安排和插件现在会切换为独立右侧工作区：PR 页提供 GitHub CLI 安装提示、命令复制和 Git 项目入口；已安排页提供搜索、推荐模板、创建与取消；插件页提供搜索、已安装项、marketplace 管理、安装与刷新。已安排任务可保存提示词与时间，在 Codex Desk 保持运行时自动切换回对应项目并创建新对话发送。待执行项会跨重启保留，任务运行中或运行时不可用时会等待重试，不会打断当前工作。
- 插件工作区现可在“插件”和“技能”间切换；技能页展示 App Server 为当前项目提供的真实技能，并按个人/项目与系统范围分组。右上角“添加”菜单新增创建插件、添加插件市场和录制技能：创建插件会在新对话预填 `$plugin-creator`，录制技能会在新对话直接启用结构化录制上下文；市场表单支持 GitHub 仓库、Git URL 与本地目录，Git 引用继续使用 Codex CLI 的默认解析。
- 修复取消已安排任务时的竞态：若任务正等待项目切换或运行时重连，取消后不会继续创建新对话或发送提示词。
- 左侧项目树不再常驻显示任务搜索框；顶部搜索图标现在会打开与 Codex 桌面端一致的“搜索聊天”面板，可跨已加载项目按任务标题、预览、项目或 Provider 查找并恢复任务，同时提供新聊天、打开文件夹和文件变更搜索快捷操作。
- 修复新建任务或切换历史任务后，旧任务迟到的生命周期和计划通知可能写入当前视图的问题；项目详情卡片的任务数现在每次打开都会刷新。
- 修复文件变更统计将仅含 Diff 头、二进制或元数据的不可计数变更显示为 `+0 -0` 的问题；现在会明确显示 `+? -?`。
- 修复连续切换任务时，已打开任务会因完成状态延迟刷新而重新显示蓝色完成提醒的问题。
- 任务名称、Provider / App Server、沙箱边界和文件变更入口现在位于右侧工作台顶部；任务名称显示当前任务，点击后可行内修改，Enter 保存、Esc 取消。
- 工作区改为左右两列主结构：左侧项目列和右侧工作台列现在各自拥有独立顶部栏；宽屏下的环境信息继续保留在工作台内，原有 Git 与文件变更操作不受影响。
- 收紧右侧“环境信息”卡片的字号：标题、操作行、分组和文件行分别使用 18/14/14/13px，提升窄侧栏的信息密度。
- 修复耗时条目新增后未处理其时间线展示类型导致应用无法编译的问题。
- 修复插件 CLI 路径解析未受超时限制的问题：自动发现路径与插件命令现在均在 20 秒后超时，避免插件页持续加载。
- 插件页现在读取官方 marketplace 插件 manifest 的展示名、简介、分类和 PNG/JPEG/SVG logo，并按官方分类展示可安装插件；安装与启停仍由本机 Codex CLI 执行。
- 修复插件管理在桌面应用 `PATH` 中找不到 `codex` 时无法加载列表的问题：插件命令现在复用运行时自动发现的 CLI 绝对路径。
- 收紧任务完成后文件变更摘要的标题和新增/删除行统计字号，避免其在对话流中显得过大。
- 收紧侧栏选中任务与任务搜索框的垂直内边距，列表扫描时不再显得过于松散。
- 对话时间线现在会显示正在运行的 Codex 命令；命令完成后自动收纳到既有活动清单，不会把持续刷新的命令输出展开成终端日志。每轮任务结束后会保留“已处理”耗时，恢复历史任务时同样可见。
- 修复已恢复任务的后台或迟到生命周期事件可能覆盖当前任务状态的问题：它们现在只刷新任务列表，不会清除当前命令/计划、写入错误耗时或污染文件变更与 Diff。
- 修复开发版工作区可能错误保存 macOS 系统临时目录的问题：测试不再写入真实偏好，应用会拒绝该目录作为项目或附加目录，并在启动恢复时自动移除既有记录。
- 所有 tooltip、项目详情卡片和文件 Diff 悬停预览现在都会在持续悬停 450ms 后显示，快速掠过控件或文件行不再弹出干扰操作的浮层。
- 修复侧栏跨项目任务预览：其他项目的缓存任务现在会先切换到所属项目并重连运行时再恢复，不能误在当前项目中继续、置顶、归档、重命名或删除。
- 文件变更协议记录不再逐条占据会话：实时与历史会话中的旧记录都会隐藏，文件数、增删行数和 Diff 仍会通过任务完成摘要、右侧检查器与“审核”入口提供。
- 修复展开多个项目后，其他项目的缓存任务会错误继承当前任务选中态与 loading 的问题；这两种状态现在只显示在当前项目。
- 修复任务运行中项目侧栏的“新建任务”按钮仍可点击的问题：按钮现在会禁用，避免显示与实际状态不符的“运行时就绪后才能新建任务”提示；完成或停止当前任务后即可新建。
- 侧栏项目树在每次启动时都会恢复并默认展开所有项目的本地任务列表；项目切换不再造成其他列表收起。需要收起时请点击整个项目行，左侧文件夹图标会同步显示关闭状态，该状态仅影响当前会话。
- 统一应用状态管理：主题偏好改由 Riverpod 持有并持久化，实时弹窗统一订阅共享控制器；局部表单、悬停、拖拽和滚动状态仍由对应 Widget 管理，用户可见行为保持不变。
- 切换侧栏任务时，首次加载会显示 Codex 风格历史加载页并缓存最近 8 个已打开任务的完整时间线页面和滚动位置；再次切换同一任务会直接显示已保活页面，任务切换和实时回复的自动定位均不再出现平滑滚动或文字随滚动出现的过渡，超出上限的较早任务会重新读取历史；历史读取失败时不会显示前一任务内容。
- 修复对话文字区域各自创建滚动容器的问题：文本仍可选择复制，但滚轮始终驱动整条对话时间线，不再出现嵌套滚动造成的文字出现过渡。
- 切换侧栏项目时，已访问项目的任务列表会保留展开状态，不再收起之前项目的任务。
- 随任务发送的图片现在会显示在对话时间线的用户消息气泡中；剪贴板生成的临时图片在时间线仍引用时不会因任务完成而提前删除。
- 已完成任务的蓝色提醒现在表示“未查看的完成结果”；打开对应任务后会自动清除提醒。
- 修复“变更与审批”中的审批模式未在重启后恢复的问题；选择“帮我批准”后，后续启动会继续自动批准命令、文件变更与额外权限请求。
- 左侧任务栏与右侧环境栏现在可通过分隔条拖拽调整宽度；悬停和拖动会提供主题强调色反馈，窄窗口仍会自动隐藏右侧栏。
- 连续的历史命令、文件读取和工具调用现在会以默认折叠的 Codex 风格活动清单显示；点击摘要可展开或再次折叠，单项保留完整事件详情提示。
- 右侧检查器改为 Codex 风格的“环境信息”圆角卡片，接入本地 Git 变更、分支、提交/推送、创建拉取请求和比较分支入口；Git 视图新增文件级暂存、二次确认后的还原、显式提交和推送，以及通过本机 `gh` CLI 创建 PR。
- Codex 回复现按 GitHub Flavored Markdown 显示，列表、加粗、链接、引用、表格与代码块不再展示原始标记；内容仍可选择复制，用户输入、命令和系统记录继续以纯文本显示。
- 修复对话时间线误把 App Server 命令输出增量等协议级执行事件显示为“执行事件”的问题；这些高频内部通知现已隐藏，审批、任务结果和恢复历史中的实际命令记录不受影响。
- 修复项目独立历史升级后的缓存迁移：若项目 ID 已写入但历史仍按旧目录索引保存，应用会恢复该缓存并立即迁移，避免重启后历史任务显示为空。
- 修复加密本地历史保存覆盖其他项目缓存的问题，并串行化历史迁移与常规保存，避免切换项目或并发保存后历史丢失。
- 修复新建项目误显示同一源目录历史任务的问题：项目现在拥有独立任务归属，新项目从空任务列表开始，后续创建的任务仅显示在所属项目中。
- 新建项目改为 Codex 风格弹窗：项目名称可选，源文件夹区域支持点击打开目录选择器，也支持直接拖入目录后创建项目。
- 项目菜单的“编辑”改为 Codex 桌面端风格的项目编辑器：支持编辑项目显示名称、管理源文件夹、添加/移除附加文件夹、取消或保存，以及移除当前本地项目记录；移除操作不会删除磁盘目录或历史缓存。
- 修复项目编辑器名称输入框的重复边框；外层项目字段保留单层边框，聚焦时不再显示主题注入的内层边框。
- 优化任务 Composer：移除随心输入、模型选择和推理强度控件的额外边框，推理强度按钮与菜单改为仅显示“默认”“高”“中”“低”等强度名称。
- 在任务完成后显示文件变更摘要卡片，并提供只读代码审查窗口：可按文件查看逐行 Diff、统计新增/删除行数和浏览长代码行。
- 当 App Server 只返回文件路径而未携带文件级 Diff 时，对未跟踪文件通过 Git 工作区只读预览补齐；无法可靠取得 Diff 时显示未知统计，避免误报 `+0 -0`。
- 审查窗口中的完整任务 Diff 不计入文件数量，单个文件 Diff 读取失败也不会阻断其他文件的预览。
- 已编辑文件摘要支持悬停预览：预览会避开窗口边缘、显示 unified Diff 的实际行号，并在文件 Diff 异步补齐或更新后同步刷新。
- 文件变更统计提示现悬浮在输入框顶缘，展示“X 个文件已更改”及新增/删除行数，不再占用独立布局高度；无法取得 Diff 时显示未知统计。
- 运行中任务支持从最新用户消息进入“调整方向”；运行期间 Composer 保持可编辑，修正指令通过 `turn/steer` 发送到当前活动 turn，停止按钮继续用于中断任务。
- 修复运行中调整方向误清空附件上下文、成功后输入框残留文本，以及 steering 后未沿用服务端返回 turn ID 的问题；运行期间现在可以继续附加文件、图片、文件夹、技能、目标和计划上下文。
- 修复 App Server 暂时返回空线程列表时历史任务无法自动恢复并导致输入区锁死的问题；应用会保留本地缓存线程作为恢复候选，并等待历史缓存加载完成后再连接运行时。
- 左侧导航调整为 Codex 风格的项目树布局：工作区使用文件夹节点，任务作为文件缩进排列，选中任务显示低对比圆角背景；悬停项目自动展示目录、当前或本地缓存任务数和路径详情卡片，详情卡片支持持久化项目置顶，点击更多按钮可打开置顶、编辑、创建永久工作树、归档聊天和移除项目菜单，点击新建任务按钮会开始新的任务，任务运行中会在当前任务行尾显示加载指示器，任务完成显示蓝色信息点，任务错误显示红色叹号。
- 修复运行中任务在侧栏缺少状态反馈的问题：当前任务现在显示更醒目的加载指示器并可点击保持焦点；新线程在 App Server 列表同步前也会保留在侧栏，避免运行中的任务行暂时消失。

## 1.0.0+1

Initial macOS release.

- Connect to the local Codex App Server and manage tasks, approvals, plugins, and workspace threads while using Codex-native model and provider configuration.
- Automatically connect restored workspaces, keep the runtime usable after a failed task, and recover startup failures or unexpected process exits with bounded 1/2/5-second retries.
- Create, retain, switch, and forget multiple workspaces, with independent primary/additional directories and local history for each entry.
- Negotiate the App Server experimental API capability required to pass additional workspace roots when starting a task.
- Show saved workspaces directly in the sidebar with stable creation order, a distinct active state, one-click switching, bounded scrolling, and separate create/manage actions.
- Cancel stale in-flight runtime startups safely during reconnect or disposal, and serialize additional-workspace persistence so older writes cannot replace newer directory selections.
- Keep encrypted local conversation history, restore workspace preferences, and provide a read-only Git change view.
- Include runtime diagnostics with credential redaction, API Key / browser sign-in, effective model/provider values and sources read from Codex configuration, and configurable reasoning effort.
- Select a model for subsequent new tasks from the App Server catalog, keep a follow-configuration option, and update reasoning-effort choices to match the selected model without overriding historical threads.
- Show live structured task plans in a floating step-progress panel with pending, active, and completed states.
- Add a reference-matched composer menu for files, folders, workspace context, persistent goals, plan mode, skill recording, and workspace-scoped App Server skills, plus ChatGPT-style desktop file dropping, Finder file/folder pasting, and clipboard screenshot pasting with thumbnail and zoomable preview support, deduplicated attachment chips, and normal text paste preservation.
- Make in-place macOS installs resilient to cancelled Automation quit requests, and request administrator privileges only when `/Applications` is not writable.
- Use Riverpod for application-level controller ownership and workspace state subscriptions, while preserving safe test and embedded-controller injection.
- Support batch archive and confirmed permanent task deletion with duplicate-submission protection.
- Package the Release application with `./build_dmg.sh`; unsigned output remains the default while Developer ID credentials are unavailable.
- Search and filter read-only Git changes, with an explicit warning when a large Diff preview is truncated.
- Show plugin-operation progress, actionable CLI failure details, and persistent runtime-restart feedback.
- Migrate the workbench theme tokens into the project and remove the external UI kit dependency; the default dark palette now follows the Codex-style graphite surfaces and semantic status colors shown in the reference UI.
- Support Developer ID signing, notarization, stapling, and Gatekeeper assessment in `build_dmg.sh` when release credentials are supplied.
- Align local development and CI on Flutter 3.47.1 / Dart 3.13.1 and migrate the macOS project to the Flutter-supported macOS 12.0 deployment target.
