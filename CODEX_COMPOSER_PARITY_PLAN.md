# Codex Composer 与任务文件一致性改造方案（当前开发目标）

> 本文档当前只覆盖仓库内可实现、可测试并已交付的开发内容。官方客户端精确行为矩阵、视觉并排验收、任务文件最终范围实测和具体 IDE 插件已移至 [CODEX_COMPOSER_PARITY_FOLLOWUP.md](CODEX_COMPOSER_PARITY_FOLLOWUP.md)，不属于本次目标。

## 目标

将当前 Composer 中由 `@` 和 `/` 触发的能力，以及任务文件在多轮对话中的展示、Diff、撤销和恢复行为，对齐到指定版本 Codex 桌面客户端的用户可见行为，并在 Codex App Server 已公开支持的范围内使用相同的协议和数据语义。

“保持一致”在本方案中包括：

- 菜单内容、顺序、分组、文案、图标和状态一致。
- 鼠标、键盘、焦点、悬停、关闭和选择后的行为一致。
- 用户选择的功能产生真实动作，不以提示词或成功提示伪造未接入能力。
- App Server 请求、结构化输入、生命周期和失败反馈尽量一致。
- 深浅色、窄窗口、任务运行中和服务端不可用等边界状态一致。
- 任务文件在单轮、多轮、任务切换和应用重启后的范围与生命周期一致。

Codex 桌面客户端的私有源码和内部实现不可作为可验证目标，因此本方案不承诺源码级一致；验收目标是可观察交互一致，以及公开协议范围内的实现一致。

## 当前交付范围

### 已接入真实动作

- `@`：文件和文件夹、当前项目、目标、计划模式、Skill。
- `/`：MCP、代码审查、归档、推理强度、模型选择、新聊天。
- Goal 会进入提交数据并持久化到线程。
- Plan 会通过 `collaborationMode` 发送给 App Server。
- Skill 会作为结构化 `type: skill` 输入发送。
- 图片会作为结构化 `localImage` 输入发送。

### 部分实现或语义不完整

- “IDE 上下文”已提供 `codex_desk/ide_context` 通用宿主通道，可接收当前文件、选区和打开标签；有效快照到达后 `/` 项启用，只有用户明确选择后才通过 `turn/start.additionalContext` 或 `turn/steer.additionalContext` 发送字符串化 JSON，断连或切换项目会清除选择。当前仓库尚无具体 IDE 插件端，且官方未公开来源键与序列化格式，因此独立运行时仍禁用，不能宣称官方 IDE 集成已经完整交付。当前项目路径继续从 `@`/添加菜单独立提供。
- `@` 非空查询已接入 App Server 0.153.4 的 `fuzzyFileSearch`，按主目录和附加工作区顺序展示真实文件/目录建议，保留系统选择器作为浏览更多入口；查询和工作区竞态、加载/错误/空状态、键盘/鼠标选择、窄窗口与符号链接越界均有处理。普通文件和目录在首轮任务和运行中方向调整里同时通过 `mention {name, path}` 结构化输入和提示词路径传递；菜单精确分组、二次排序与官方桌面视觉仍待实测。
- “录制技能”曾通过选择 `skill-creator` 并追加提示实现，但这不是真正的操作流程录制；当前已禁用，等待公开协议或官方实测证据。
- “代码审查”已通过结构化 `review/start` 接入，但仍需确认是否覆盖官方完整审查生命周期。
- 目标、计划和 Skill 已接入真实数据；Goal 的状态记录和运行中目标摘要已补齐，Skill 列表刷新时会重新校验选中路径，但仍需核对官方客户端的精确生命周期、失败恢复和展示规则。
- 同一 thread 启动后续回合时不再清空既有 `fileChanges` 和最近确认的 `turnDiff`；无文件事件的追问完成后任务文件仍可见并继续持久化，切换任务或应用重启后可从本地加密快照恢复。新聊天仍从空摘要开始。同一 turn 内的聚合 Diff 更新会替换该 turn 的派生文件，后续 turn 则保留既有 thread 摘要；累计文件集合超出最近 turn Diff 覆盖范围时安全撤销保持禁用。该保留策略修复了已复现的数据丢失，但最终展示范围仍需官方桌面实测确认是最新 turn、整个 thread 还是当前 Git 工作树。
- App Server Schema 已确认 `turn/diff/updated` 的 `diff` 是“该 turn 内所有 file change 的最新聚合 Diff”，而 `thread/read`、`thread/resume` 和 `thread/turns/list` 的历史项按 turn 返回 Diff；这支持当前按 turn 替换 Diff、按 thread 保留文件摘要的实现，但仍不能单独证明官方桌面右侧任务文件的最终展示范围。
- 本地快照同时按 thread 保存最近的文件摘要与 Diff，使任务切换和重启恢复不再只依赖当前活动任务的全局文件集合；跨轮展示范围仍以官方桌面实测为准。
- 安全撤销完成时会按发起任务核对 Diff；即使执行期间已切换到其他任务，也会同步清除原任务的页面缓存和按 thread 持久化快照并安排保存，避免切回或重启后恢复已撤销的旧摘要。
- `@`/`/` 菜单在打开和查询变化后会把默认选择归一到首个可执行项，禁用项不会截获 Enter；鼠标悬停可用行会同步键盘选择，保持两种输入方式指向同一动作。
- 未选择项目时，`@` 与添加菜单的当前项目上下文入口统一禁用并展示原因，不会再清空查询后静默无操作。
- Composer MCP 面板已改用 `mcpServerStatus/list` 和 `mcpServerStatus/updated` 的线程级实时状态；CLI MCP 列表仅用于设置管理，不再冒充运行时连接状态。
- 侧边聊天创建期间会禁用重复 fork，并在任务切换、进入审查或已有侧边聊天时释放迟到结果；审查模式和嵌套入口保持禁用。

### 已接入的公开协议能力

- 侧边聊天已通过 ephemeral fork 和独立面板接入；审查模式、嵌套场景和精确焦点规则仍待实测。
- 创建聊天分支已通过 `thread/fork` 接入并切换到服务端返回线程；工作树选择语义仍待实测。
- 主动压缩上下文已通过 `thread/compact/start` 接入，并在确认后进入真实运行状态。
- 反馈已通过 `feedback/upload` 接入，诊断日志须用户明确勾选。

## 实施原则

1. 先记录官方行为，再修改本地实现，禁止根据印象猜测交互。
2. 官方行为矩阵是进入实现阶段的硬门槛；没有官方证据的条目只能保留为待验证项，不能直接转化为实现要求。
3. 不保留能够点击但只产生假成功结果的入口。
4. App Server 不支持的能力，必须按照目标 Codex 版本的实际行为隐藏、禁用或明确降级。
5. 服务端能力、异步状态、缓存和跨界面共享状态优先使用 Riverpod。
6. 仅属于 Composer 当前实例的光标、悬停和临时选中状态可以保留为局部状态。
7. 新增或迁移的 Dart 类遵守一类一文件、公开命名、禁止 `part` 隐藏类的项目规则。
8. 所有用户可见变化完成后同步更新 `README.md`、`ROADMAP.md` 和 `RELEASE_NOTES.md`。

## 阶段一：建立官方基线

### 1.1 固定目标版本

记录：

- Codex 桌面客户端版本和构建号。
- 操作系统版本。
- Codex App Server 版本。
- 使用的模型和账号能力范围。
- 是否连接 IDE、MCP 和其他插件。

后续改造和验收均以该版本为准。Codex 升级后，应重新执行差异审计，而不是默认新版本行为不变。

### 1.2 建立交互矩阵

分别记录 `@` 和 `/` 在以下状态下的表现：

- 空白新聊天。
- 已存在历史消息的聊天。
- 任务运行中。
- 任务已完成。
- 没有打开项目。
- 已连接和未连接 IDE。
- App Server 离线或重连中。
- MCP、Skill 或模型列表为空、加载中和加载失败。
- 第一轮修改文件、后续轮次只追问但不修改文件。
- 多轮连续修改同一文件或不同文件。
- 存在任务开始前的 Git 工作区改动。
- 切换聊天、切换项目和应用重启后的恢复。

每项记录：

- 入口是否显示。
- 菜单中的顺序、分组、图标、标题和说明。
- 可用、禁用、隐藏和加载状态。
- 默认聚焦项。
- 点击或键盘选择后的输入框内容。
- 焦点落点和光标位置。
- 产生的面板、对话框、任务或服务端请求。
- 成功、失败、取消和重试行为。

### 1.3 建立任务文件专项基线

在目标版本官方 Codex 客户端中按固定顺序执行并记录：

1. 在干净工作区开始新聊天，第一轮创建一个文件。
2. 第二轮只发送追问，不修改任何文件。
3. 第三轮再次修改第一轮创建的文件。
4. 第四轮创建另一个文件。
5. 分别触发官方提供的审查、撤销或恢复入口。
6. 切换到其他聊天后再切回。
7. 退出并重新打开客户端。
8. 在任务开始前预置一项 Git 改动，重新执行上述流程。

每一步必须记录：

- 右侧“任务文件”和 Composer 文件变更提示显示的文件集合。
- 时间线是否保留每一轮独立的文件变更。
- 同一文件跨轮修改时显示最新 Diff、累计 Diff，还是多份 turn Diff。
- 撤销作用于当前 turn、整个 thread，还是当前工作树。
- 任务生成的改动与任务开始前已有 Git 改动是否分开显示。
- 切换任务和重启后恢复的数据范围。
- App Server 的 `fileChange`、`turn/diff/updated` 及相关历史数据能否支持该行为。

完成实测前，不得预先选择“最新 turn”“整个 thread”或“当前 Git 工作树”作为本地数据模型。

### 1.4 建立协议矩阵

在允许的范围内记录每项功能对应的：

- App Server 方法。
- 请求参数和结构化输入。
- 返回结果。
- 通知事件。
- 生命周期状态。
- 可恢复错误和不可恢复错误。

如果官方文档没有公开某项协议，以目标版本客户端的可观察行为作为交互基线，同时明确记录协议未知，不推测私有实现。

### 1.5 阶段验收

- 形成唯一的官方行为矩阵。
- 所有待改菜单项均能映射到明确的官方证据。
- 每个差距被标记为交互差距、协议差距或运行时能力缺失。
- 任务文件范围、Diff 合并、撤销和恢复语义均有逐步实测记录；无法观察或协议未知的部分明确标记为未知。
- 阶段一完成并经过人工确认前，不进入阶段二及后续实现。

## 阶段二：统一命令和能力模型

### 2.1 命令定义

为每个 Composer 动作建立统一、可测试的定义：

- 稳定标识。
- 触发入口：`@`、`/` 或两者。
- 标题、说明、图标和搜索别名。
- 排序和分组。
- 显示条件。
- 启用条件。
- 所需 App Server 能力。
- 执行动作类型：本地动作、结构化提交或服务端请求。
- 官方版本兼容范围。

### 2.2 能力状态

统一表达：

- `loading`：正在确认能力。
- `available`：能力真实可执行。
- `disabled`：能力存在，但当前会话状态不允许执行。
- `unsupported`：当前 App Server 未提供。
- `failed`：能力读取失败，可以重试。

菜单是否显示 `unsupported` 项，必须由官方行为矩阵决定，不能为了展示完整列表而固定保留。

### 2.3 状态边界

使用 Riverpod 管理：

- App Server 能力列表。
- MCP、模型和 Skill 的异步加载状态。
- 当前线程是否允许归档、分支、压缩和侧边聊天。
- 跨 Composer、面板和对话框共享的命令执行状态。

保留在局部 Widget 中：

- 当前菜单选中项。
- 悬停项。
- 当前查询文本。
- 菜单是否被本次 Esc 临时关闭。

### 2.4 阶段验收

- 菜单不再混合硬编码展示和分散执行逻辑。
- 每个可点击项都有真实执行器。
- 禁用原因可测试且不会被键盘选中。
- 异步结果不会写入已切换的线程或工作区。

## 阶段三：对齐 `@` 入口

### 3.1 触发与搜索

核对并实现：

- `@` 是否只能位于输入开头，还是可以位于句中。
- 查询范围是否从最近的合法 `@` 到当前光标。
- 空格、换行、中文输入法和组合文字对触发范围的影响。
- 多个连续提及的行为。
- 搜索字段、排序、模糊匹配和无结果状态。

### 3.2 文件和目录

- 对齐官方的文件搜索、选择器或建议列表。
- 保留项目边界和符号链接安全检查。
- 能使用结构化文件上下文时，不再仅把路径写入提示词。
- 区分文件、目录、图片和临时剪贴板附件。
- 对齐重复选择、失效文件、权限错误和移除行为。

### 3.3 IDE 上下文

先在目标版本客户端记录“IDE 上下文”实际包含的数据字段，并确认对应公开协议。候选字段包括但不限于：

- 当前选区。
- 当前文件。
- 光标位置。
- 打开的文件。
- IDE 工作区信息。

实现时只把官方实测确认的字段列为一致性要求。如果 IDE 未连接或协议没有提供数据，入口的隐藏、禁用和说明应与官方一致。当前仅附加项目路径的行为必须标记为降级，不得在没有证据时宣称等同于官方 IDE 上下文。

当前实现提供公开边界内的通用宿主接口，支持 `activeFile`、`activeSelectionContent` / `selectedText`、`selectionRange` 和 `openTabs`；最多保留 64 个打开标签，选区最多 64,000 字符。App Server Schema 要求 `additionalContext` 每项的 `value` 为字符串，因此快照使用 JSON 字符串传入首轮与方向调整请求；宿主连接本身和官方不透明来源格式仍是待完成的集成项。

宿主接入契约见 [IDE_CONTEXT_HOST_PROTOCOL.md](IDE_CONTEXT_HOST_PROTOCOL.md)，包含 `updateContext` 示例、断连语义、用户选择边界和隐私责任。该契约不表示仓库已经内置某个 IDE 插件。

### 3.4 当前项目

- 明确“当前项目”和“IDE 上下文”是两个不同能力。
- 对齐官方是否仅传递工作目录，还是附加项目级上下文引用。
- 多根工作区需要保持根目录顺序、范围和安全边界。

### 3.5 Goal、Plan 和 Skill

- Goal 的进入、取消、草稿恢复、提交、持久化和后续消息行为一致。
- Plan 的可用状态、模型依赖、运行中禁用和 `collaborationMode` 参数一致。
- Skill 的加载、分组、详情、选择、徽标和结构化提交一致。
- Skill 被禁用、删除或路径失效时，提交前必须重新校验。

### 3.6 录制技能

- 确认官方“录制技能”的真实输入、过程和输出。
- 若存在 App Server 或客户端协议，接入真实录制生命周期。
- 核对官方入口名称是否确实为“录制技能”，以及官方是否同样通过 Skill 和提示语义完成该动作。
- 若本地只能调用 `skill-creator`，根据官方实测结果决定保留名称、改名、隐藏或禁用，不能提前预设结论。
- 如果目标版本使用专用录制能力而当前运行时不支持，不得用提示词模拟已录制。

### 3.7 阶段验收

- `@` 中每个可选项都产生可验证的真实上下文或模式状态。
- IDE 上下文包含真实 IDE 数据，或严格遵循官方降级行为。
- 文件、Goal、Plan 和 Skill 的提交数据都有协议测试。

## 阶段四：对齐 `/` 入口

### 4.1 已有真实命令

逐项核对并调整：

- IDE 上下文。
- MCP。
- 代码审查。
- 归档。
- 推理强度。
- 模型选择。
- 新聊天。

核对内容包括显示条件、选择后输入框内容、是否打开内联面板、是否立即执行、取消行为、错误反馈和重试行为。

### 4.2 代码审查

- 对齐未提交更改和相对分支审查的入口。
- 对齐分支加载、空状态、失败和重试。
- 确认官方是否发送普通任务、结构化审查请求或进入独立审查模式。
- 对齐运行状态、审查结果面板、文件跳转和恢复历史。
- 不能只以最终提示词文字相似作为实现一致的依据。

### 4.3 缺失命令

以下能力已接入，当前阶段重点是官方桌面行为验收：

- 侧边聊天：`thread/fork` 的 ephemeral 独立面板。
- 创建聊天分支：持久 `thread/fork` 并切换到返回线程。
- 主动压缩上下文：确认后调用 `thread/compact/start`。
- 反馈：通过 `feedback/upload` 提交用户选择的日志偏好。

处理顺序：

1. 检查目标 App Server 是否已有对应接口。
2. 若需要升级 App Server，先完成版本和兼容性改造。
3. 接入真实请求、通知、错误和恢复生命周期。
4. 如果服务端确实不支持，按照官方客户端在相同条件下的行为隐藏或禁用。

不得继续保留“点击后只显示尚未支持”但外观与正常可用命令完全相同的入口，除非官方客户端本身就是该行为。

### 4.4 阶段验收

- `/` 菜单中的每个可点击项都能完成对应真实动作。
- 所有命令的显示和禁用状态与官方行为矩阵一致。
- 不支持能力没有虚假成功反馈。

## 阶段五：对齐键盘、焦点与视觉

### 5.1 键盘

- `ArrowUp` / `ArrowDown` 移动选择。
- `Enter` 和 `Tab` 的选择语义。
- `Esc` 的分层关闭顺序。
- Shift、输入法组合和候选确认期间不误执行。
- 菜单关闭后输入内容和光标位置正确恢复。
- 禁用项不会通过键盘获得可执行焦点。

### 5.2 鼠标和悬停

- 鼠标悬停与键盘焦点状态同步。
- 点击后只执行一次。
- 滚动期间不会错误切换选中项。
- 超长菜单自动滚动到当前选中项。

### 5.3 视觉

- 菜单宽度、最大高度、圆角、阴影和与 Composer 的间距。
- 行高、图标、标题、辅助说明和分组标题。
- 默认、悬停、焦点、按下、禁用和加载状态。
- 深浅色表面和对比度。
- 超长名称和多语言文本。
- 窄窗口、窗口边缘和 Composer 高度变化。
- 减少动态效果设置。

### 5.4 阶段验收

- 在固定客户端版本、系统版本、主题、字体缩放和设备像素比下，使用相同逻辑窗口宽度进行截图对照。
- 菜单边界、行高、间距、图标边界和焦点框与官方基线的几何偏差不超过 1 个逻辑像素；系统字体抗锯齿产生的像素差异通过文字区域遮罩排除。
- 颜色使用项目中与官方截图匹配的主题 Token；不存在肉眼可见的错误表面、截断、重叠或状态差异。
- 自动截图差异报告和人工并排验收均通过，并在官方行为矩阵中记录验收人、日期和目标版本。
- 键盘、鼠标和输入法行为通过自动化测试。
- Overlay 在窗口边界和生命周期切换时没有异常。

## 阶段六：协议与失败行为

每个命令统一遵循以下执行链：

```text
菜单选择
  → 本地和服务端能力检查
  → 本地状态变更或 App Server 请求
  → loading / success / error
  → Composer 内容、焦点和菜单状态恢复
```

需要覆盖：

- 服务端未启动。
- 服务端执行中断。
- 请求超时。
- 当前线程在请求期间被切换。
- 当前工作区被移除。
- 能力列表刷新后命令失效。
- 重复点击和并发请求。
- 迟到结果和过期请求。
- App Server 返回未知错误或新状态。

反馈载体应与官方一致，不能统一使用 Snackbar 替代面板错误、行内错误或禁用说明。

上下文用量已改为消费 App Server 0.153.4 的 `thread/tokenUsage/updated`：使用 `last.totalTokens / modelContextWindow` 展示最近一次模型上下文占比，按 thread/turn 隔离迟到与后台通知；缺少有效窗口时明确等待服务端数据，不再以字符数或固定 258k 估算。

## 阶段七：对齐任务文件生命周期

### 7.1 根据官方证据选择数据范围

阶段一完成后，从以下模型中选择官方实测对应的一种，或按证据组合：

- 最新 turn 文件变更。
- 整个 thread 累计文件变更。
- 当前 Git 工作树变更。
- thread 累计摘要与逐 turn Diff 并存。

决策必须记录官方证据和 App Server 数据来源，不允许仅根据“任务文件”文案推断。

### 7.2 数据与展示

- 将线程级摘要、逐 turn Diff 和 Git 工作区状态定义为不同数据边界，只有官方实测证明相同含义时才可合并。
- 新一轮开始时是否清空、保留或暂时隐藏旧变更，严格按照官方实测结果实现。
- 同一文件跨轮修改的合并规则、文件重命名、删除、二进制文件和未跟踪文件均需覆盖。
- 右侧“任务文件”、Composer 变更提示、时间线摘要、审查面板和撤销入口使用一致的数据语义。
- 切换聊天、项目和应用重启时，按官方范围恢复，并拒绝其他线程或项目的迟到事件。

### 7.3 撤销和安全边界

- 撤销范围必须在执行前可确定，并与官方行为一致。
- 不得把当前工作区中任务开始前的用户改动错误归入任务 Diff 或一并撤销。
- Diff 缺失、截断、路径有歧义或工作区已变化时，按照官方行为禁用、提示或要求刷新。

### 7.4 阶段验收

- 第一轮改文件、第二轮只追问的结果与官方客户端一致。
- 多轮修改同一文件和不同文件的摘要、Diff 与官方一致。
- 任务前已有 Git 改动不会被错误归属或撤销。
- 切换聊天、切换项目和重启后的恢复与官方一致。
- 不再由既有“单轮范围”测试预先决定产品语义；测试根据已确认的官方基线重写。

## 阶段八：测试与验收

### 8.1 Widget 测试

- `@` 和 `/` 的合法、非法触发位置。
- 连续输入和查询筛选。
- 鼠标、键盘、Enter、Tab 和 Esc。
- 禁用项跳过。
- 菜单自动滚动。
- 焦点和光标恢复。
- IME 组合输入。
- 空、加载、错误和重试状态。
- 第一轮有文件变更、下一轮无文件变更时的官方预期展示。
- 多轮修改相同文件和不同文件。

### 8.2 协议测试

每个真实命令验证：

- 调用的 App Server 方法。
- 请求参数。
- 结构化输入。
- collaboration mode。
- 线程和 turn 标识。
- 成功、失败和重试行为。
- `fileChange` 与 `turn/diff/updated` 在多轮对话中的归属和恢复。
- `thread/tokenUsage/updated` 的 thread/turn 归属、最近请求用量、缺失窗口与无效数据降级。

### 8.3 生命周期测试

- 请求期间切换聊天。
- 请求期间切换项目。
- Composer 销毁和重建。
- App Server 停止和重连。
- 异步结果迟到。
- 运行中任务和历史任务的状态隔离。
- 带文件变更的线程在切换和重启后的恢复。
- 任务开始前已有 Git 改动与任务生成改动的隔离。

### 8.4 视觉回归

仓库已新增 `test/composer_menu_golden_test.dart` 及三张本地基线（深色命令菜单、浅色提及菜单、窄窗口深色提及菜单），用于锁定当前实现的几何和状态回归；这些基线不是官方客户端截图，不能替代官方并排验收。

覆盖：

- 深色和浅色。
- 常规、狭窄和窗口边缘宽度。
- 超长 Skill、模型和命令名称。
- 长菜单滚动。
- 运行中禁用状态。
- 加载、空和错误状态。
- 任务文件为空、单轮变更和多轮变更状态。

### 8.5 提交前验证

执行：

```bash
dart format .
flutter analyze
flutter test <相关测试文件>
flutter test
```

## 阶段九：文档同步

完成用户可见改造后：

- 更新 `README.md` 中 Composer 的实际行为和限制。
- 更新 `ROADMAP.md` 唯一任务清单，将已交付能力归入对应“已完成”小节。
- 更新 `RELEASE_NOTES.md` 未发布版本，记录新增、修复和行为变化。
- 删除把未实现、提示词模拟或路径降级描述为完整官方能力的文案。

文档只描述最终实现，不记录临时调试过程或尚未交付的设想。

## 推荐交付顺序

1. 固定官方客户端和 App Server 版本。
2. 完成官方行为与协议矩阵。
3. 建立统一命令和能力模型。
4. 对齐 `@` 的文件、IDE 上下文、Goal、Plan 和 Skill。
5. 对齐 `/` 中已经可执行的真实命令。
6. 验收已接入的侧边聊天、聊天分支、主动压缩和反馈，并补齐官方边界状态。
7. 根据专项官方基线对齐任务文件的数据范围、Diff、撤销和恢复。
8. 完成键盘、焦点、悬停、窄窗口和视觉对齐。
9. 完成协议、生命周期和视觉回归测试。
10. 更新项目文档并执行完整验证。

## 本次目标完成定义

只有同时满足以下条件，才可将 Composer 与任务文件一致性任务标记为完成：

- 仓库内定义的 `@` 和 `/` 条目均已实现真实动作，或在缺少运行时/宿主能力时正确隐藏、禁用。
- 所有可点击入口都产生真实动作。
- 不再用普通提示词模拟官方宣称的专用能力，除非官方本身采用该语义。
- App Server 支持的能力使用经过协议测试的真实请求。
- 鼠标、键盘、焦点、输入法、窗口边界和异步生命周期通过测试。
- 本地深浅色、窄窗口和关键生命周期回归基线通过测试；官方视觉并排改由后续清单验收。
- 多轮文件变更、撤销、任务切换和重启恢复按公开 App Server 语义实现并通过测试；官方最终展示范围改由后续清单确认。
- `README.md`、`ROADMAP.md` 和 `RELEASE_NOTES.md` 与当前实现同步。
- `dart format`、`flutter analyze` 和完整相关 `flutter test` 全部通过。

## 后续文档

官方客户端实测、视觉并排、任务文件最终范围和具体 IDE 插件见 [CODEX_COMPOSER_PARITY_FOLLOWUP.md](CODEX_COMPOSER_PARITY_FOLLOWUP.md)。这些事项不阻塞本次仓库开发目标的完成。

## 已知边界

- OpenAI 官方文档和 Codex 私有实现未必公开所有桌面交互细节，需要以指定版本客户端的可观察行为作为最终依据。
- 侧边聊天、聊天分支、主动压缩和反馈是否能严格实现，取决于当前或可升级 App Server 是否提供对应协议。
- 如果官方客户端展示某项能力，但 App Server 对第三方客户端不开放，则必须在“升级运行时、采用公开替代协议、按官方降级隐藏”之间作出明确选择，不能伪造完成。
- Codex 客户端升级可能改变菜单和协议，因此需要保留可重复执行的一致性审计和视觉回归流程。
